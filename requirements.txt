# PDF2MD — المتطلبات
# التثبيت:  pip install -r requirements.txt
#
# الحدود العليا تمنع المشغّلين (run.sh / run.bat) من سحب إصدار رئيسي
# مستقبلي كاسر تلقائيًا من PyPI.

# الأرضية مرفوعة عمدًا: الأداة تحلّل ملفات PDF غير موثوقة عبر MuPDF وهي
# مكتبة C، فأخطاء التحليل فيها تعني تلف ذاكرة لا استثناء بايثون. الأرضية
# المنخفضة كانت تسمح بتثبيت إصدار من ٢٠٢٤ صدرت بعده عشرات الإصلاحات.
PyMuPDF>=1.26,<2   # قراءة PDF بإحداثيات كل حرف (rawdict) ورسم الصفحة لفحص الحبر
numpy>=1.24,<3     # قياس كثافة الحبر داخل الفجوات
PyQt6>=6.6,<7      # الواجهة الرسومية (غير لازمة لتشغيل سطر الأوامر)
