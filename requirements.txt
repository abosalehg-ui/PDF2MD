# PDF2MD — المتطلبات
# التثبيت:  pip install -r requirements.txt

PyMuPDF>=1.24     # قراءة PDF بإحداثيات كل حرف (rawdict) ورسم الصفحة لفحص الحبر
numpy>=1.24       # قياس كثافة الحبر داخل الفجوات
PyQt6>=6.6        # الواجهة الرسومية (غير لازمة لتشغيل سطر الأوامر)
