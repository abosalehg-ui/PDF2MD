# مراجعة مِحَك — PDF2MD

> النوع المكتشف: **تطبيق سطح مكتب** (CLI + واجهة PyQt6) — رابرك التطبيقات: هندسة ٤٥٪ + UX/UI ٣٠٪ + أمن ٢٥٪
> تاريخ المراجعة: 2026-08-16 — على الإصدار `2.0.0`، آخر commit `706d78e`
> مراجعة ثانية بعد تنفيذ خطة الإصلاح السابقة (`مراجعة-PDF2MD.md`، درجتها كانت 7.5/10)

---

## 1. نظرة عامة سريعة

أداة تستخرج نصًا عربيًا سليمًا من ملفات PDF المعطوبة — الملفات المصدَّرة من Word ثم المعالَجة عبر محرّك Quartz تحمل خللًا في خريطة `ToUnicode` يخزّن الرباطات العربية معكوسة، فتخرج «املادة» بدل «المادة». المنهج هندسي بحت: إحداثيات الحروف على الصفحة + قياس بكسلات الصفحة المرسومة، بلا قاموس ولا تخمين لغوي.

- **التقنيات:** Python ≥3.9، `PyMuPDF ≥1.24,<2` (الاستخراج والرسم)، `numpy ≥1.24,<3` (قياس الحبر)، `PyQt6 ≥6.6,<7` (الواجهة، اختيارية).
- **الحجم:** ١٤ ملف مصدري، ~1,830 سطر بايثون + ٣٣١ سطر اختبارات: `gui.py` (716)، `core.py` (491)، `structure.py` (415)، `cli.py` (152)، `main.py` (71)، `common.py` (38)، ومشغّلان `run.sh`/`run.bat`.
- **نطاق المراجعة:** المستودع كاملًا، كل سطر قُرئ فعليًا. الاختبارات شُغّلت: **٢٤ اختبارًا كلها تعبر** في ٠٫٣٥ ثانية.
- **ملاحظة منهجية:** كل ملاحظة في هذا التقرير مثبَتة بتشغيل فعلي على ملفات PDF مولَّدة أثناء المراجعة، لا بقراءة الكود وحدها. **لكن** دقة الثوابت نفسها (هل `GAP_RATIO = 0.13` صحيحة على مستندات متنوعة، وهل عتبة الحبر `0.06` تصمد على خطوط أخرى) **لا يمكن تقييمها من الكود وحده** — تحتاج مجموعة ملفات حقيقية متنوعة ومقارنة يدوية بالمخرجات. التقرير يحكم على الهندسة والبنية والسلوك، لا على دقة الخوارزميات على الملفات الحقيقية.

**التقدّم منذ المراجعة السابقة:** الاختبارات انتقلت من صفر إلى ٢٤ اختبارًا مع CI فعلي، `common.py` أزالت التكرار الثلاثي بين الواجهتين، `src` صارت حزمة حقيقية، والهروب من HTML أُضيف في تبويب التشخيص. تقدّم حقيقي وليس تجميليًا.

---

## 2. المراجعة التقنية والهندسية (45%)

### 2.1 البنية المعمارية وفصل المسؤوليات — 9/10

الطبقات نظيفة فعلًا: `core.py` (هندسة الحروف والإحداثيات) ← `structure.py` (البنية و Markdown) ← `cli.py`/`gui.py` (عرض فقط) ← `common.py` (المشترك بين الواجهتين). `core.py` ما يعرف شي عن Markdown، والواجهتان ما تعرفان شي عن الإحداثيات، وكل الإعدادات تمرّ عبر `dataclass` واحد. المنطق الثقيل في `QThread` منفصل بإشارات. هذا فصل صحيح ومنضبط.

المأخذ الوحيد الحقيقي: **علم مهجور**. `core.build_lines()` يحسب لكل سطر:

```python
ordered = sorted(seg_boxes, key=lambda b: -b[1])
is_row = len(ordered) >= 4 and all(
    ordered[k][0] - ordered[k + 1][1] >= CELL_GAP
    for k in range(len(ordered) - 1)
)
...
lines.append({..., "row": is_row})
```

وبحثت في المستودع كله: **لا أحد يقرأ `"row"` إطلاقًا**. `structure.py` ما يستعمله، ولا `cli.py`، ولا `gui.py`. يعني كشف صفوف الجداول محسوب في كل سطر من كل صفحة ثم يُرمى، والثابت `CELL_GAP` موجود في جدول المعايرة في README وموثّق للمستخدم كأنه فعّال. النتيجة: الجداول تخرج مسطّحة في فقرة واحدة (أثبتها الاختبار في القسم 2.4)، وREADME يعترف بها كـ«حدّ معروف» بينما الكود يحمل نصف الحل مدفونًا.

**الحل:** إما استهلك العلم في `structure.convert()` — سطر `row=True` يُجمَّع مع جيرانه في جدول Markdown حقيقي بـ `|` — أو احذف `is_row` و`CELL_GAP` من `core.py` ومن جدول README. الأسوأ من الاثنين هو الوضع الحالي: كود ميت يوهم القارئ أن الميزة موجودة.

ملاحظة ثانية أخف: ما فيه `pyproject.toml`. المشروع الآن حزمة صحيحة لكن ما يمكن `pip install .` ولا توزيعه على PyPI ولا تثبيته في venv نظيف — كل التشغيل معتمد على الوقوف في جذر المشروع.

### 2.2 قابلية القراءة والصيانة — 8/10

الشروحات من أفضل ما قرأت في مستودع عربي: كل docstring يشرح **ليش** القاعدة موجودة، مع مثال التلف الفعلي المرصود («يقتض ي → يقتضي»، «يوما.ً → يوماً.»). مطوّر جديد يفهم المنطق من التعليقات وحدها بلا حاجة للمؤلف. هذا نادر.

المأخذ الباقي من المراجعة السابقة وما تغيّر: `build_lines()` في `core.py` لسه دالة واحدة تسوي خمس مهام (كشف صف الجدول، ربط التشكيل، الترتيب، إسقاط مسافات الأرقام، تجميع النص مع فحص الحبر)، وحلقة التجميع فيها أربع حالات متشابكة مع متغيّر «الجبهة»:

```python
parts, prev, pending, front = [], None, False, None
for u in group:
    if u.is_mark:
        pending = True
        continue
    if prev is not None and not pending and not u.glue:
        gap = (front - u.x1) if rtl else (u.x0 - front)
```

منطق `front` صحيح وموثّق بعناية، لكنه أصعب جزء في المستودع كله، وما يحرسه اختبار مباشر — الاختبارات الموجودة تصله عبر `text_of()` فقط.

وانضاف عليه: `structure.convert()` صار ~١٥٠ سطرًا يسوي أربع مراحل داخل دالة واحدة (استخراج، قياسات المستند، بناء، تجميع)، مع حلقة داخل حلقة داخل حلقة. المراحل معلّمة بتعليقات `── ١) الاستخراج ──` وهذا يساعد، لكن التعليق الفاصل عادةً علامة على أن الدالة تطلب تفكيكًا.

**الحل:** فكّك حلقة التجميع إلى `_join_units(group, rtl, ink, z, band_y0, band_y1)` قابلة للاختبار وحدها، وفكّك `convert()` إلى `_extract_pages()` و `_build_markdown()` و `_assemble()`. كل واحدة تصير قابلة للاختبار بمعزل.

### 2.3 التكرار (DRY) و Code Smells — 8.5/10

التكرارات الثلاثة التي رصدتها المراجعة السابقة **أُغلقت فعلًا**: `verdict_of()` و `out_path_for()` و `stats_summary()` كلها في `common.py` الآن ويستدعيها الطرفان. تحقّقت من ذلك في `cli.print_diag()` و `gui.show_diag()` — نفس المصدر.

الباقي تكرار واحد حقيقي وخطير أكثر مما يبدو: **مفتاح تطبيع الترويسة مكرر ثلاث مرات نصًّا** في `structure.py` — مرة في `repeated_headers()`:

```python
counter.update({
    re.sub(r"\d+", "#", l["text"]) for l in lines
    ...
})
```

ومرتين داخل `convert()` عند الفحص:

```python
(rel < HEADER_ZONE and (re.sub(r"\d+", "#", t) in boiler ...
or (rel > FOOTER_ZONE and re.sub(r"\d+", "#", t) in boiler)
```

المشكلة مو الطول، المشكلة **الاقتران**: الدالة التي تبني المجموعة والكود الذي يبحث فيها لازم يتفقان حرفيًا على صيغة التطبيع. لو عدّل أحد `\d+` إلى `[\d٠-٩]+` في مكان واحد فقط (وهذا تعديل معقول جدًا لأن الترويسات العربية فيها أرقام هندية!)، حذف الترويسات يتوقف بصمت — بلا خطأ ولا تحذير، فقط ترويسات تظهر في الناتج.

**الحل:** دالة واحدة في `structure.py`:

```python
def boiler_key(text):
    """مفتاح مقارنة الترويسة: الأرقام مجرّدة لأنها تتغيّر كل صفحة."""
    return re.sub(r"[\d٠-٩]+", "#", text)
```

واستدعها في الثلاثة المواضع.

### 2.4 معالجة الأخطاء والحالات الحدّية — 5.5/10

أضعف قسم في المشروع، وفيه ثلاث مشاكل مثبَتة بالتشغيل.

**أ) 🔴 الكتابة خارج `try` — تسقط الدفعة كلها بـ traceback خام.** في `cli.main()`:

```python
try:
    md, st = convert(pdf, opt, log=say)
except Exception as e:
    print(f"✗ فشل تحويل ...", file=sys.stderr)
    failed.append(pdf)
    continue
with open(out, "w", encoding="utf-8") as f:      # ← خارج الحماية
    f.write(md)
```

الحماية تغطي التحويل فقط. الكتابة مكشوفة. شغّلت هذا فعليًا:

```
python main.py doc.pdf -o /tmp/nodir/x.md --no-ink -q
→ FileNotFoundError: [Errno 2] No such file or directory: '/tmp/nodir/x.md'
```

انهيار بـ traceback بايثون خام، بعد ما اشتغل التحويل كاملًا وضاع الناتج. ونفس الشي يصير مع قرص ممتلئ أو صلاحية مرفوضة أو مسار للقراءة فقط. المفارقة أن `README` و`cli.py` نفسه يعلنان أن «الملف الفاشل يُتخطّى ولا يوقف البقية» — وهذا صحيح لفشل التحويل، وغير صحيح لفشل الكتابة. الوعد مكسور في نصفه.

**السبب الجذري:** `common.out_path_for()` يستدعي `os.makedirs(folder, exist_ok=True)` في فرع المجلد فقط، أما فرع الملف الصريح (`-o x.md`) فيرجع المسار كما هو بلا إنشاء أي مجلد.

**الحل:** انقل الكتابة داخل `try` نفسه، وأنشئ مجلد الأب قبلها:

```python
try:
    md, st = convert(pdf, opt, log=say)
    os.makedirs(os.path.dirname(os.path.abspath(out)), exist_ok=True)
    with open(out, "w", encoding="utf-8") as f:
        f.write(md)
except Exception as e:
    print(f"✗ فشل تحويل {os.path.basename(pdf)}: {e}", file=sys.stderr)
    failed.append(pdf)
    continue
```

**ب) 🟠 نطاق صفحات خارج المدى يتحوّل بصمت إلى «الملف كله».** في `structure.convert()`:

```python
lo = max(0, (opt.page_from or 1) - 1)
hi = min(n - 1, (opt.page_to or n) - 1)
if hi < lo:
    lo, hi = 0, n - 1          # ← سقوط صامت
```

شغّلته على ملف من صفحتين بالخيار `--pages 500-600`، والنتيجة: `pages = 2` — حوّل الملف كاملًا بدل ما يقول «النطاق خارج المدى». المستخدم اللي طلب صفحات ٥٠٠–٦٠٠ من كتاب ظنّه ألف صفحة، وطلع ١٢٠ صفحة، يستلم الكتاب كله وهو يظن أنه استلم الجزء المطلوب. الاختبار `test_convert_reversed_page_range_falls_back_to_all` **يثبّت هذا السلوك كأنه ميزة**، وهو في الحقيقة يخفي خطأ المستخدم.

**الحل:** افصل الحالتين — نطاق مقلوب (خطأ صياغة) يُرفض في `cli.options_from()` وهو مرفوض فعلًا هناك، أما نطاق خارج المدى فيُقصّ مع تحذير صريح:

```python
if lo >= n:
    raise ValueError(f"بداية النطاق ({opt.page_from}) تتجاوز عدد الصفحات ({n}).")
if hi < lo:
    hi = n - 1
    say(f"⚠ نهاية النطاق أصغر من بدايته — حُوّل الملف كاملًا ({n} صفحة).")
```

**ج) 🟠 `out_path_for()` تنشئ مجلدات كأثر جانبي — والواجهة تنشئها قبل موافقة المستخدم.** الدالة اسمها «مسار المخرَج لـ»، ومتوقّع منها حساب سلسلة نصية، لكنها تكتب على القرص:

```python
folder = out if out else (os.path.dirname(os.path.abspath(pdf)))
os.makedirs(folder, exist_ok=True)     # ← أثر جانبي في دالة حسابية
```

تحقّقت: مجرد استدعائها ينشئ المجلد فعلًا. والمشكلة تظهر في `gui.run_convert()` الذي يستدعيها لمجرد **الفحص**:

```python
existing = [o for o in map(self.out_for, self.all_files())
            if os.path.exists(o)]
```

يعني لو المستخدم كتب مجلد حفظ ثم ضغط «تحويل» ثم ضغط «لا» على سؤال الكتابة فوق الملفات — المجلد صار موجودًا على قرصه أصلًا. وأخطر: لو `makedirs` فشلت (صلاحية مرفوضة، مسار غير صالح، قرص ممتلئ)، الاستثناء يُرفع **داخل slot في Qt**، وPyQt6 يعامل الاستثناء غير المُمسك في slot بـ `qFatal` — يعني إنهاء التطبيق فورًا بلا رسالة مفهومة، مع ضياع قائمة الملفات والإعدادات.

**الحل:** خلِّ `out_path_for()` حسابية بحتة (احذف `makedirs`)، وانقل الإنشاء إلى موضع الكتابة الفعلي في `cli.main()` و `gui.ConvertWorker.run()` — والأخيرة تسوّيه أصلًا. ثم لفّ جسم `run_convert()` بـ `try/except` يعرض `QMessageBox.critical` بدل ما يوقع التطبيق.

**ما هو جيد فعلًا:** فحص `doc.needs_pass` برسالة عربية واضحة (ومُختبَر)، الإلغاء عبر `threading.Event` مع استثناء مخصّص `ConversionCancelled` مفصول عن حالات الخطأ، `try/finally` حول `doc.close()` في `convert` و`diagnose` معًا، إبقاء مراجع `QThread` حيّة في `self._workers` مع تعليق يشرح سبب الانهيار لو ما سُوّي، و`closeEvent` ينتظر الخيوط بدل ما يتلفها وهي شغّالة. هذه ليست تفاصيل يلتقطها مطوّر متوسط.

### 2.5 الأداء واستهلاك الموارد — 8/10

القرارات الأساسية سليمة: `Unit` تستعمل `__slots__` (توفير حقيقي مع عشرات الآلاف من الحروف)، خريطة الحبر تُرسم **مرة واحدة لكل صفحة** لا مرة لكل فجوة، `numpy` يقيس الكثافة بعملية متجهية واحدة، وفحص الحبر مقصور على الفجوات بين حرفين عربيين.

قِسته فعليًا على ملف من ٢٠ صفحة: `check_ink=False` ← ٠٫٠١ ثانية، `check_ink=True` ← ٠٫٠٣ ثانية. أي ٣× تمامًا كما يعد README. الوعد دقيق.

الملاحظات الباقية:

1. **`_group_segments()` تربيعية في عدد الأجزاء** — لكل جزء تمرّ على كل المجموعات السابقة وعلى كل صناديقها. صفحة عادية فيها عشرات الأجزاء فلا مشكلة، لكن صفحة جدول كثيف فيها مئات الأجزاء تدخل في `O(n²)` مع فحص تداخل داخلي. مقبول عمليًا، لكن يستحق تعليقًا يحدّد الحدّ الأعلى المتوقّع.

2. **`diagnose()` يشغّل فحص الحبر بلا داعٍ ولا مهرب** — يستدعي `core.page_lines(doc[i], st)` بالقيم الافتراضية، و`check_ink=True` هو الافتراضي. يعني «الفحص السريع» يرسم كل صفحة من العيّنة بدقة ١٥٠ نقطة. على كتاب ٩٠٠ صفحة العيّنة = ١٢٩ صفحة = ١٢٩ عملية رسم. وأسوأ: `diagnose()` **ما تقبل `cancel`**، والواجهة تعطّل زر «إيقاف» أثناء الفحص (`busy(True)` بلا `stoppable`) — فالمستخدم عالق حتى ينتهي.

   **الحل:** مرّر `check_ink=False` في `diagnose` (الفحص التشخيصي يقيس تلف الرباطات، وهذا لا علاقة له بالمسافات الوهمية)، وأضف معامل `cancel` لها كما في `convert`، ومرّره من `DiagWorker`.

3. **الذاكرة** — أسطر المستند كله تُحمّل قبل البناء، وهذا **مذكور صراحة في «الحدود المعروفة»** بالعربي مع تبرير صحيح (حساب حجم المتن الغالب والترويسات المتكررة يحتاج المستند كاملًا). التوثيق الصريح للحدّ يرفع الدرجة، لا ينقصها.

### 2.6 الاختبارات — 6/10

قفزة حقيقية من صفر. ٢٤ اختبارًا تعبر كلها، موزّعة على ثلاثة ملفات بمنهج ذكي: `test_core.py` يبني `rawdict` مصطنعًا عبر `FakePage` (لأن إنتاج ملف بخلل `ToUnicode` حقيقي يتطلب حقن content stream يدويًا — والسبب موثّق في docstring الملف)، بينما `test_structure.py` و`test_cli.py` يولّدان **ملفات PDF حقيقية** بـ `fitz` أثناء التشغيل. والحالات المختارة معنوية لا تجميلية: الرباط المقلوب مع إحصاءاته، الملف المشفّر، الإلغاء، صمود الدفعة أمام ملف تالف، الكتابة فوق ملف موجود بـ`--force` وبدونه. و CI يشغّلها على بايثون ٣٫٩ و٣٫١٢.

لكن الفجوات كبيرة ومحدّدة:

| ما هو غير مُختبَر | الحجم | الخطورة |
|---|---|---|
| **نمط `saudi_law` كاملًا** — صفر إشارة له في كل ملفات الاختبار | ٥ تعابير نمطية + فرع `classify` كامل + منطق `in_laiha` | 🔴 هذا هو النمط الرئيسي المعلَن في README وفي أمثلة CLI |
| **فحص الحبر `_inked` / `ink_map`** | القاعدة رقم ٣ في README | 🟠 كل اختبارات البنية تمرّر `check_ink=False` — القاعدة الأغلى في المشروع بلا حارس |
| `anchor_of` / توليد الفهرس | — | 🟠 وفيه خطأ حقيقي غير مكتشَف، انظر ٢٫٦-ب |
| توجيه الحواشي (`quote`/`inline`/`drop`) + دمج تكملة الحاشية | ~٢٠ سطرًا | 🟠 |
| `is_toc_page` / تخطّي الفهرس الأصلي | — | 🟡 |
| `gui.py` كاملًا | ٧١٦ سطرًا = **٣٩٪ من الكود** | 🟠 مستثنى من CI صراحة |

`saudi_law` هو الفجوة الأخطر: المشروع كله معنون «استخراج الأنظمة واللوائح السعودية»، والمثال الأول في README هو `--profile saudi_law`، والفرع فيه خمسة تعابير نمطية متشابكة وحالة `in_laiha` تعبر بين الأسطر — ولا اختبار واحد يمسّه. أي تعديل على `RE_MADDA` أو `RE_BAB` يمرّ في CI أخضر.

**ب) 🟠 خطأ حقيقي كشفَته الفجوة: مراسي الفهرس مكسورة.** `structure.anchor_of()`:

```python
return re.sub(r"[^\w\s؀-ۿ-]", "", text).strip().replace(" ", "-")
```

شغّلت التحويل على ملف فيه عنوانان متطابقان بالإنجليزية، والناتج:

```markdown
## الفهرس
- [Chapter One](#Chapter-One)
- [Chapter One](#Chapter-One)
```

مشكلتان معًا:

1. **ما فيه تصغير حروف.** مراسي Markdown في GitHub وفي أغلب المصيّرات **تُصغَّر دائمًا** — العنوان `Chapter One` مرساه الحقيقي `#chapter-one`، والرابط المولَّد `#Chapter-One` **لا يفتح شيئًا**. كل عنوان فيه حرف لاتيني كبير = رابط ميت. والعناوين العربية سليمة (لا يوجد فيها حالة أحرف)، فالعطل يظهر فقط في المستندات المختلطة — وهي شائعة في الأنظمة السعودية (أسماء جهات ومصطلحات إنجليزية).

2. **ما فيه فضّ تعارض.** عنوانان بالنص نفسه ← مرساة واحدة، فكل روابط الفهرس تذهب للأول والبقية غير قابلة للوصول. وهذا **متوقّع جدًا** في المستندات القانونية: «الفصل الأول» يتكرر تحت كل باب.

**الحل:**

```python
def anchor_of(text, seen=None):
    a = re.sub(r"[^\w\s؀-ۿ-]", "", text).strip().replace(" ", "-").lower()
    if seen is None:
        return a
    seen[a] = seen.get(a, 0) + 1
    return a if seen[a] == 1 else f"{a}-{seen[a] - 1}"
```

ومرّر قاموسًا واحدًا أثناء بناء الفهرس في `convert()`، مع استعمال المرساة نفسها في العنوان لو أردت ضمانًا كاملًا.

**ج) 🟠 عيب جوهري في نمط `auto` كشفَته التجربة.** `structure.body_font_size()` يحسب حجم المتن الغالب **موزونًا بعدد الحروف** — والتبرير في التعليق منطقي («حتى لا يطغى عشرون عنوانًا قصيرًا على مئة سطر متن»). لكن الترجيح ينقلب على صاحبه لما يكون في الصفحة نص صغير كثيف.

بنيت صفحة فيها سطرا متن بـ 12pt وجدول من سبعة صفوف بـ 9pt، والناتج:

```markdown
### Main body paragraph one here.
### Main body paragraph two here.

Row 0 | Value 0 | Note 0 Row 1 | Value 1 | Note 1 ...
```

**المتن صار عناوين، والجدول صار المتن.** لأن الجدول (٧ صفوف × ~٣٠ حرفًا = ٢١٠ حرفًا) يغلب المتن (٥٨ حرفًا) بالوزن، فـ`body_size = 9`، وكل ما هو 12pt يتجاوز `body_size + 1.2` فيُصنَّف `sub`. وهذا بالضبط شكل الصفحة في المستند القانوني المحشو بالجداول والحواشي — وهو الاستعمال المستهدَف.

**الحل:** أضف حارسًا مطلقًا — حجم المتن لا يكون أصغر من وسيط أحجام الأسطر (median) بفارق كبير، أو رجّح بعدد الحروف **مع سقف لكل سطر** حتى لا يهيمن سطر واحد طويل:

```python
weight[round(l["size"] * 2) / 2] += min(len(l["text"]), 80)
```

ثم تحقّق: لو الحجم المختار أصغر من وسيط الأحجام بأكثر من نقطتين، ارجع للوسيط.

**الحل العام للقسم:** أضف `tests/test_saudi_law.py` يغطي الباب/الفصل/المادة/اللائحة التنفيذية، وثلاثة اختبارات لـ`anchor_of` (تصغير، تعارض، عربي)، واختبارًا واحدًا لـ`_inked` بوحدات مصطنعة وخريطة `numpy` يدوية، واختبارًا يثبّت أن نمط `auto` ما ينقلب على صفحة غالبها نص صغير.

### 2.7 التوثيق — 9/10

من أفضل ما رأيت في مستودع بهذا الحجم. README ثنائي اللغة كاملًا (٣٨١ سطرًا)، يشرح **المشكلة** بجدول قبل/بعد ملموس، ثم **كل قاعدة** بمنطقها وأرقام معايرتها ومصدر تلك الأرقام («٢٨ ألف فجوة»)، ثم جدول الثوابت، ثم **الحدود المعروفة** بصراحة تامة. الـdocstrings في الكود تكمّله ولا تكرّره.

ثلاث ملاحظات صغيرة:

1. **تناقض صريح:** README يقول «`src` حزمة بايثون عادية ... فتُستورد من جذر المشروع مباشرة **بلا أي تلاعب بـ `sys.path`**»، بينما `main.py` يسوّي `sys.path.insert(0, HERE)` بالضبط. التلاعب مبرَّر وموثّق في docstring `main.py` (للتشغيل من مجلد آخر)، لكن README ينفيه. صحّح الجملة إلى: «الاستيرادات نسبية داخل الحزمة؛ `main.py` وحده يضيف جذر المشروع إلى `sys.path` ليعمل من أي مجلد».

2. **قسم «الحدود المعروفة» الإنجليزي ناقص بندين** موجودين في العربي: حدّ الذاكرة على الكتب الضخمة، والملفات المحمية بكلمة مرور. القارئ الإنجليزي يستلم صورة أوردى.

3. **`conftest.py` فارغ تمامًا (صفر بايت) بلا سطر تعليق واحد.** وجوده ضروري — هو الذي يضع جذر المشروع على `sys.path` فتنجح `from src import core` في الاختبارات. أي مطوّر يرى ملفًا فارغًا سيحذفه ظنًّا أنه بقايا، فتنكسر كل الاختبارات دفعة واحدة برسالة `ModuleNotFoundError` غامضة. اكتب فيه سطرين:

```python
# -*- coding: utf-8 -*-
"""موجود عمدًا وفارغ: وجوده في الجذر يجعل pytest يضيف جذر المشروع
إلى sys.path، فتعمل `from src import core` في كل الاختبارات. لا تحذفه."""
```

### 2.8 اتفاقيات التسمية ونمط موحّد — 8/10

`snake_case` مطّرد في كل الكود، الثوابت `UPPER_CASE` ومجمّعة في رأس كل ملف مع شرح، البادئة `_` للدوال الداخلية (`_inked`, `_seg_box`, `_group_segments`) مستعملة باتساق، التقسيم البصري بالخطوط `═══` واضح ومتسق عبر الملفات الخمسة.

المآخذ:

1. **أسماء بحرف واحد في مسارات ساخنة:** `B = Builder(opt)` و`st` و`l` و`u` و`t` و`z` و`d`. الحرف `l` تحديدًا مخالفة معروفة (`E741: ambiguous variable name 'l'`) لأنه يلتبس بالرقم ١ في كثير من الخطوط — ومستعمل في `structure.convert()` وفي `body_font_size()` وفي `repeated_headers()`. `B` أسوأ: متغيّر محلي بحرف كبير مفرد يخالف PEP 8 مخالفة مباشرة.

2. **لا يوجد أي ضابط آلي للنمط:** لا `ruff`، لا `flake8`، لا `black`، ولا `.editorconfig`، وCI يشغّل `pytest` فقط. يعني الاتساق الحالي كله بانضباط شخصي من المؤلف — وهو انضباط ممتاز فعلًا، لكنه لا يصمد مع مساهم ثانٍ.

**الحل:** بدّل `l` → `line` و`B` → `builder`، وأضف `ruff` بإعداد من ثلاثة أسطر في `pyproject.toml` وخطوة `ruff check .` في CI. تكلفة عشر دقائق تقفل الباب نهائيًا.

### درجة القسم الهندسي

(9 + 8 + 8.5 + 5.5 + 8 + 6 + 9 + 8) ÷ 8 = **7.75 ≈ 7.8/10**

---

## 3. المظهر والتصميم وتجربة المستخدم (30%)

المشروع له واجهتان — رسومية (PyQt6) وسطر أوامر — فأقيّم الاثنتين معًا، وأوضّح متى عدّلت المعيار لسطر الأوامر كما يقتضي الرابرك.

### 3.1 الاتساق البصري — 8.5/10

لوحة ألوان معرَّفة بأسماء في رأس `gui.py` (`BROWN`, `GOLD`, `CREAM`, `INK`, `LINE`, `OK`, `BAD`, `PANEL`) ثم تُحقن في `QSS` واحد عبر f-string. خطان محدَّدان بوضوح ووظيفة: `Amiri` للعناوين (`QLabel#title`, `QGroupBox::title`) و`Cairo` للمتن و`Consolas` للنص المبرمج. أنصاف الأقطار موحّدة (`8px` للعناصر، `10px` للحاويات)، والحالات مغطّاة (`:hover`, `:disabled`, `:selected`). هذا نظام تصميم حقيقي لا ألوان مبعثرة.

الشرخ الوحيد: **نظاما ألوان في تطبيق واحد.** بعد كل هذا الانضباط، `gui.show_diag()` يرجع للألوان المدمجة في Qt:

```python
item.setForeground(Qt.GlobalColor.red)
...
item.setForeground(Qt.GlobalColor.darkGreen
                   if not r["after_bad"] else Qt.GlobalColor.red)
```

بينما `OK = "#2E7D52"` و`BAD = "#B3261E"` معرَّفان فوق ومستعملان في `diag_info` في **الدالة نفسها**. النتيجة: أحمر Qt الفاقع (`#FF0000`) بجانب أحمر اللوحة الهادئ (`#B3261E`) في الشاشة نفسها.

**الحل:** `item.setForeground(QColor(BAD))` و`QColor(OK)` — سطران.

### 3.2 سهولة الاستخدام والتنقّل — 7.5/10

المهمة الأساسية تُنجَز في خطوتين: اسحب الملف ← اضغط «تحويل». الافتراضات كلها مضبوطة مسبقًا فالمستخدم العادي ما يلمس أي خيار. الاختصارات موجودة وموثّقة في التلميحات نفسها (`Ctrl+O` إضافة، `Ctrl+D` فحص، `Ctrl+Return` تحويل، `Esc` إيقاف) — وهذه لمسة احترافية: التلميح يعلّم الاختصار بدل ما يخفيه في قائمة. الطابور الدُفعي وقائمة السحب والإفلات مع منع التكرار (`have = set(self.all_files())`) وعرض اسم الملف مع المسار الكامل في التلميح — كلها قرارات صحيحة.

المآخذ:

1. **لا حفظ للإعدادات إطلاقًا.** ما فيه `QSettings` ولا أي تخزين. المستخدم الذي يحوّل أنظمة سعودية يوميًا لازم يختار `saudi_law` ويكتب مجلد الحفظ **في كل تشغيل**. هذا أكثر احتكاك يومي في التطبيق كله، وحلّه ~١٥ سطرًا.

2. **زر «تفريغ» بلا تأكيد ولا تراجع.** `b_clr.clicked.connect(self.files.clear)` — نقرة واحدة تمسح قائمة من ٤٠ ملفًا جُمّعت بالسحب، بلا سؤال وبلا استرجاع. الرابرك يطلب «تأكيد قبل الحذف»، والتطبيق يطبّقه فعلًا للكتابة فوق الملفات لكن ينساه هنا.

3. **الفحص التشخيصي على «الملف الحالي» بلا إبراز.** `current_pdf()` يرجع العنصر المحدَّد أو الأول، والزر مكتوب عليه «فحص تشخيصي» فقط — ما يقول أي ملف. في قائمة من ١٠ ملفات، يضغط المستخدم ويستلم نتيجة ما يدري لأي ملف (اسم الملف يظهر في السجل فقط، وهو تبويب ثالث ما هو مفتوح).

**الحل:** `QSettings` للإعدادات، `QMessageBox.question` قبل التفريغ لو العدد > ٣، ونصّ الزر ديناميكي: `f"فحص: {os.path.basename(pdf)}"`.

**سطر الأوامر — ممتاز:** `--help` عربي كامل مع `choices` تمنع القيم الخاطئة قبل التشغيل، فصل صحيح بين `stdout` و`stderr`، رموز `✓`/`✗`/`⚠` تفرّق الحالات بصريًا، رمز خروج `1` عند الفشل الجزئي (فيصلح للأتمتة)، وجدول `--diag` منسّق بأعمدة محاذاة. المأخذ الوحيد هو الـtraceback الخام في ٢٫٤-أ.

### 3.3 الاستجابة — 8/10

معيار سطح المكتب لا الجوال: `QSplitter` بنِسَب أولية معقولة (`[440, 760]`)، اللوحة اليسرى داخل `QScrollArea` بـ`setWidgetResizable(True)` فالخيارات ما تُقصّ عند تصغير النافذة، `setMinimumSize(940, 620)` يناسب شاشات المحمول الشائعة، و`QHeaderView.ResizeMode.Stretch` يوزّع أعمدة جدول التشخيص تلقائيًا. `setStretchFactor` مضبوط بحيث تتمدد لوحة المعاينة وتثبت لوحة الخيارات — وهذا هو السلوك الصحيح.

المأخذ: `self.preview.setPlainText(md[:200000])` — القطع عند ٢٠٠ ألف حرف قرار أداء سليم (قطع الحماية من تجميد `QPlainTextEdit`)، لكنه **صامت**. المستخدم اللي حوّل كتابًا من ٦٠٠ ألف حرف يشوف المعاينة تنتهي فجأة في منتصف جملة، وما فيه ما يقول إن الملف المحفوظ كامل. سطر واحد يحلّها:

```python
if len(md) > 200000:
    self.preview.appendPlainText(
        f"\n\n… [المعاينة مقطوعة عند 200,000 حرف — الملف المحفوظ كامل ({len(md):,} حرفًا)]")
```

### 3.4 العربية و RTL — 9/10

فحصت هذا فعليًا لا افتراضًا، والتنفيذ من أنضج ما يكون:

- `setLayoutDirection(RightToLeft)` مطبَّق على **ثلاث طبقات**: التطبيق (`app`)، والنافذة (`MainWindow`)، وحقلَي النص العربي (`preview`, `diag_sample`) — والأخيران مهمّان لأن `QPlainTextEdit` يرث الاتجاه لكن محاذاة النص داخله تحتاج ضبطًا صريحًا.
- عنوان `QGroupBox` منقول لليمين يدويًا: `subcontrol-origin: margin; right: 14px` — وهذه بالضبط النقطة التي تُنسى في ٩٠٪ من الواجهات العربية، فيبقى العنوان يسارًا داخل إطار RTL.
- سلاسل الخطوط فيها بدائل مضمونة على كل نظام: `"Amiri", "Scheherazade New", "Traditional Arabic", serif` و`"Cairo", "Segoe UI", "Tahoma", "Noto Sans Arabic", sans-serif`.
- توحيد الأرقام الهندية ← العربية خيار صريح للمستخدم (`AR2WEST` في `core.py`) لا فرض.
- كل النصوص الظاهرة عربية فصيحة بلا خلط.

مأخذان صغيران:

1. **الخطوط مُسمّاة لا مُضمَّنة.** على جهاز بلا `Amiri` ولا `Cairo` (وهو الحال الافتراضي على ويندوز نظيف)، الواجهة تسقط بصمت إلى `Tahoma`/`Segoe UI` — تشتغل، لكن الهوية البصرية الموصوفة في README («بخط Amiri للعناوين و Cairo للمتن») تختفي كليًا والمستخدم ما يدري ليش شكلها غير الصور.
2. **تعارض بين QSS والكود:** `title.setFont(QFont("Amiri", 26, QFont.Weight.Bold))` يضبط ٢٦pt برمجيًا، بينما `QLabel#title` في QSS يضبط `font-size: 30px`. القاعدتان تتنازعان على العنصر نفسه (QSS يفوز عادة) — احذف إحداهما.
3. **خلط في شكل الأرقام داخل الصف الواحد:** اللصيقة `(٠ = الكل)` مكتوبة برقم هندي، بينما `QSpinBox` بجانبها يعرض `0` بالأرقام العربية الغربية. في الصف نفسه، بفارق ٢٠ بكسل.

### 3.5 إمكانية الوصول — 6/10

**ما تحسّن فعلًا:** جدول التشخيص ما عاد يعتمد على اللون وحده — القيم تحمل رمزًا نصيًا مع اللون:

```python
f"✗ {r['before_bad']}" if r["before_bad"] else r["before_bad"],
f"✓ {r['after_bad']}" if not r["after_bad"] else f"✗ {r['after_bad']}"
```

وهذا يفي بمعيار WCAG 1.4.1 (لا تنقل المعنى باللون وحده). وحسبت تباين النص الفرعي `#7A6A55` على `#FAF6EE` فطلع **4.90:1** — يعبر AA للنص العادي (٤٫٥) بهامش صغير لكنه يعبر.

**الفجوات الحقيقية:**

1. **صفر أسماء وصول.** بحثت في `gui.py` كله: لا `setAccessibleName` ولا `setAccessibleDescription` ولا `setBuddy` — ولا مرة واحدة. قارئ الشاشة يقرأ `QListWidget` كـ«قائمة» بلا اسم، و`QProgressBar` بلا وظيفة، والتبويبات الثلاثة بلا وصف. والأخطر أن اللصائق في `QGridLayout` (`QLabel("نمط المستند")` مقابل `self.cb_profile`) **غير مرتبطة برمجيًا** بحقولها — الارتباط بصري فقط عبر الشبكة، فقارئ الشاشة يعلن «صندوق تحرير» بلا اسم.

   **الحل:** `label.setBuddy(self.cb_profile)` لكل زوج، و`setAccessibleName` للعناصر الرئيسية الخمسة. أقل من عشرين سطرًا.

2. **لا تصميم لحالة التركيز.** بحثت عن `:focus` في `QSS` — **صفر ورود**. يعني مؤشّر التركيز هو الافتراضي الباهت لنمط النظام، فوق خلفية كريمية مسطّحة بلا حدود قوية. المستخدم الذي يتنقّل بـ`Tab` وحده (وهذا هو المستخدم المستهدَف بمعيار الوصول) يفقد موضعه بين ستة مربّعات اختيار متتالية. أضف:

```css
QPushButton:focus, QCheckBox:focus, QComboBox:focus, QLineEdit:focus,
QSpinBox:focus, QDoubleSpinBox:focus, QListWidget:focus {
    border: 2px solid {GOLD};
}
```

3. **الحالة المعطَّلة غير مقروءة عمليًا.** `QPushButton:disabled { background: #C4B8A6; color: #F2ECE0; }` — حسبت التباين: **1.66:1**. WCAG يستثني العناصر المعطَّلة رسميًا، فهذه ليست مخالفة، لكن ٦٦٪ فوق الحد الأدنى المطلق يعني أن نص الزر يكاد يختفي أثناء التحويل — والمستخدم ما يعود يعرف أي الأزرار موجودة أصلًا. ارفع لون النص إلى `#6F6355` (تباين ~٣٫١:١) وتبقى الحالة واضحة أنها معطّلة.

### 3.6 التغذية الراجعة — 8/10

طبقات التغذية الراجعة مكتملة بشكل لافت: شريط تقدّم بنسبة، شريط حالة برسالة الخطوة الحالية (`استخراج ص 12 / 340`)، تبويب سجل تفصيلي يستقبل رسائل `log` من طبقة التحويل نفسها، رسائل إحصائية بعد كل ملف، وتأكيد صريح قبل الكتابة فوق ملفات موجودة يعرض **أسماءها** (أول ثمانية ثم `…`)، وملخّص نهائي يفرّق بصريًا بين النجاح الكامل (`information`) والفشل الجزئي (`warning`). ورسائل الخطأ للمستخدم مفهومة وعربية («الملف محمي بكلمة مرور — أزل الحماية أولًا») بينما الـtraceback التقني ينزل في السجل — وهذا هو التقسيم الصحيح تمامًا.

المأخذان:

1. **التقدّم لكل ملف لا للدفعة.** في تحويل ١٠ ملفات، الشريط يمشي ٠→١٠٠ عشر مرات، وما فيه أي مؤشّر «الملف ٣ من ١٠». المستخدم اللي شغّل دفعة كبيرة وراح يشرب قهوة يرجع ويشوف الشريط عند ٤٠٪ ولا يدري: هل هذا الملف الثاني أو التاسع؟

   **الحل:** احتفظ بـ`self._batch_total` و`self._batch_done`، واعرض `f"[{done+1}/{total}] {msg}"` في `on_progress`، أو احسب النسبة الكلية: `int((done * 100 + pct) / total)`.

2. **المعاينة تُستبدَل بصمت.** `on_done` يسوّي `setPlainText(md[...])` لكل ملف، فناتج الملف الأول يختفي فور انتهاء الثاني بلا أثر. أضف على الأقل اسم الملف في رأس المعاينة أو في عنوان التبويب.

### درجة القسم

(8.5 + 7.5 + 8 + 9 + 6 + 8) ÷ 6 = **7.83 ≈ 7.8/10**

---

## 4. المراجعة الأمنية (25%)

**تحديد النطاق أولًا:** أداة سطح مكتب أوفلاين، بلا خادم، بلا قاعدة بيانات، بلا مصادقة، بلا أي اتصال شبكي في وقت التشغيل، وبلا تخزين لأي بيانات مستخدم. فالصفوف التالية من الرابرك **لا تنطبق** ولن أخترع لها ثغرات: الصلاحيات والمصادقة، الجلسات و JWT، CORS والترويسات الأمنية، HTTPS، تجزئة كلمات المرور، الحقن في SQL. الدرجة محسوبة من الصفوف المنطبقة فقط.

سطح الهجوم الحقيقي هنا شيء واحد: **الأداة تُغذّى ملفات PDF غير موثوقة من الإنترنت**، وتمرّرها إلى مكتبة C. هذا هو المحور الذي يستحق التدقيق.

### 4.1 المفاتيح والأسرار — سليم ✓

مسحت الكود كله والتاريخ (١١ commit): لا مفاتيح، لا رموز، لا `.env`. الورود الوحيدة لكلمة `secret` هي `page.insert_text(..., "secret")` داخل اختبار توليد ملف مشفّر، و`doc.needs_pass` وهو فحص وليس سرًّا. `.gitignore` يغطي `__pycache__/` و`*.pyc` و`.pytest_cache/`. التطبيق لا يرسل أي شيء لأي جهة، ولا يجمع تحليلات، ولا يكتب سجلات خارج جلسة التشغيل.

### 4.2 الحقن وتنفيذ الأوامر — سليم ✓ مع ملاحظة 🟡

لا `eval`، لا `exec`، لا `os.system`، ولا `subprocess` إطلاقًا في المستودع. `QSS` مبني بـf-string لكن كل قيمه ثوابت وحدة معرَّفة في رأس الملف، صفر منها من إدخال المستخدم — فلا حقن أنماط.

والأهم: **الهروب من HTML مطبَّق في الموضع الصحيح.** `gui.show_diag()` يعرض بيانات PDF الوصفية داخل `RichText`، وهي نص خارجي غير موثوق يتحكم فيه من أنشأ الملف:

```python
pairs = html.escape("، ".join(f"{k}×{v}" for k, v in d["pairs"]) or "—")
...
f"<b>المنتج:</b> {html.escape(d['producer'] or '—')} ..."
```

الثلاثة كلها مهرَّبة (`producer`, `creator`, `pairs`). صحيح ومكتمل.

**🟡 الثغرة الباقية في المسار نفسه:** `QMessageBox` يستعمل `Qt::AutoText` افتراضيًا، أي يكتشف HTML ويصيّره. وفي `gui.next_job()` تُبنى الرسالة من مسارات الملفات:

```python
msg += "تم إنشاء الملفات:\n" + "\n".join(self.results)
```

ملف اسمه `<b>تقرير</b>.pdf` يُعرض بخط عريض، واسم فيه `<img src="file:///...">` يجعل Qt يحاول تحميل ملف محلي داخل الرسالة. الخطورة عملية منخفضة (المستخدم هو من يختار ملفاته)، لكن الإصلاح سطر واحد ويغلق الباب في السيناريو الوحيد المعقول — مجلد ملفات وصل من طرف آخر:

```python
box = QMessageBox(self)
box.setTextFormat(Qt.TextFormat.PlainText)
```

نفس المعالجة تنطبق على رسالة تأكيد الكتابة فوق الملفات في `run_convert()`.

### 4.3 التحقق من المدخلات — جيد مع فجوة 🟡

المطبَّق فعلًا: `--pages` يمرّ على تعبير نمطي صارم `^\s*(\d+)\s*-\s*(\d+)\s*$` مع رفض النطاق المقلوب، `--profile` و`--footnotes` مقيّدان بـ`choices` فيرفضهما `argparse` قبل أي تنفيذ، وجود الملفات يُفحص قبل بدء الدفعة، الواجهة تفلتر السحب والإفلات على `.pdf` فقط، والنطاق المقلوب مرفوض في الواجهة أيضًا برسالة صريحة (اتساق جيد بين الواجهتين). والتشفير يُكتشف صراحة بـ`doc.needs_pass` في `convert` و`diagnose` معًا.

**🟡 الفجوة:** لا حدّ لحجم الملف ولا لعدد الصفحات. ملف PDF بعشرة آلاف صفحة (أو «قنبلة PDF» مصمَّمة لذلك) يجعل الأداة ترسم عشرة آلاف صورة نقطية بدقة ١٥٠ نقطة وتحمّل أسطر المستند كله في الذاكرة — استنزاف موارد على جهاز المستخدم نفسه. ليست ثغرة عن بُعد، لكنها تعليق للتطبيق بلا تحذير. أضف في `convert()` تحذيرًا (لا رفضًا) فوق حدّ معيّن:

```python
if n > 2000:
    say(f"⚠ مستند ضخم ({n} صفحة) — الاستهلاك سيكون عاليًا. فكّر في --pages.")
```

**🟡 وملاحظة على الحدّ الأدنى للتحقق:** `structure.convert()` يقبل `opt.h_top` و`opt.h_sub` كما هما ويبني `"#" * level`. الواجهة تحدّهما بـ`QSpinBox` (١–٥ و١–٦)، لكن `cli.py` يمرّر `--h-top` كـ`int` بلا حدّ. `--h-top 400` ينتج عنوانًا بأربعمئة `#`. ضرر تجميلي فقط، لكن `choices=range(1,7)` تقفلها.

### 4.4 الاعتماديات — 🟠 أهم ملاحظة أمنية في التقرير

هنا سطح الهجوم الحقيقي. الأداة وظيفتها تحليل ملفات PDF غير موثوقة، و`PyMuPDF` غلاف على `MuPDF` وهي مكتبة **C** — أي أن أخطاء تحليل PDF فيها تاريخيًا تعني تجاوز مخزن مؤقت (buffer overflow) وتلف ذاكرة، لا مجرد استثناء بايثون. والحدّ في `requirements.txt`:

```
PyMuPDF>=1.24,<2
```

الحدّ **الأعلى** مبرَّر ومشروح في التعليق (يمنع سحب إصدار رئيسي كاسر) وهذا قرار صحيح. لكن الحدّ **الأدنى** هو المشكلة: `>=1.24` يسمح لمثبِّت يحلّ الاعتماديات بتثبيت `1.24.0` نفسه — وهو إصدار من ٢٠٢٤ صدرت بعده عشرات الإصلاحات في MuPDF، بعضها في مسار تحليل PDF بالضبط. والمستخدم الذي يشغّل `run.sh` على جهاز فيه `PyMuPDF 1.24.0` مثبَّت مسبقًا **لن يُحدَّث إطلاقًا**، لأن السكربت يفحص الاستيراد فقط:

```bash
if ! "$PY" -c 'import fitz, numpy, PyQt6' >/dev/null 2>&1; then
```

نجح الاستيراد ← لا تثبيت ولا ترقية. فيبقى على إصدار قديم إلى الأبد.

**الحل:** ارفع الأرضية إلى إصدار حديث مستقر (`PyMuPDF>=1.26,<2`)، وأضف فحص إصدار داخل `main.py` بجانب فحص الوجود:

```python
import fitz
if tuple(int(x) for x in fitz.VersionBind.split(".")[:2]) < (1, 26):
    print("⚠ إصدار PyMuPDF قديم — رقّه: pip install -U PyMuPDF", file=sys.stderr)
```

وفعّل Dependabot بملف `.github/dependabot.yml` من ست أسطر ليفتح PR تلقائيًا عند صدور إصلاح أمني.

**🟠 ملاحظة توافق مرتبطة، وهي عطل مؤكَّد قادم:** شغّلت المستودع فتبيّن أن `PyMuPDF 1.28.2` يطبع:

```
warning: The `fitz` API is deprecated and will be removed in future.
Use `import pymupdf` instead.
```

والمستودع يستورد `fitz` في **أربعة ملفات**: `src/core.py`, `src/structure.py`, `tests/test_cli.py`, `tests/test_structure.py`. والحدّ `<2` **لا يحمي** — الإزالة معلَنة داخل خط `1.x` نفسه، فأول `1.29` أو `1.30` تسقط الاسم وتنكسر الأداة كليًا عند أول `pip install -r requirements.txt` على جهاز جديد. وأسوأ: `main.py` يفحص المتطلبات بالاسم القديم `CORE_DEPS = [("fitz", "PyMuPDF"), ...]`، فبعد الإزالة سيعلن «متطلبات ناقصة: PyMuPDF» بينما المكتبة مثبَّتة سليمة — رسالة خطأ تقود المستخدم في الاتجاه الخاطئ تمامًا.

**الحل** (عشر دقائق، ويحافظ على التوافق الخلفي):

```python
try:
    import pymupdf as fitz
except ImportError:                 # PyMuPDF < 1.24.3
    import fitz
```

في `core.py` و`structure.py` وملفات الاختبار، وبدّل `CORE_DEPS` إلى `[("pymupdf", "PyMuPDF"), ("numpy", "numpy")]` مع نفس الاحتياط.

### 4.5 سلسلة التوريد والتشغيل — 🟡

`run.sh` و`run.bat` يشغّلان `pip install -r requirements.txt` تلقائيًا عند أول تشغيل، مع سقوط إلى `pip install --user` لو فشل التثبيت العام. هذا **قرار تجربة مستخدم ممتاز** لجمهور غير تقني (نقرة مزدوجة وتشتغل)، لكن له ثلاث تبعات أمنية تستحق الذكر:

1. **التثبيت خارج بيئة افتراضية** — الحزم تنزل على مستوى النظام أو المستخدم، فتختلط باعتماديات مشاريع أخرى وقد تخفضها أو ترفعها بلا علم المستخدم.
2. **بلا تثبيت بالتجزئة (hashes)** — لا `--require-hashes` ولا ملف قفل. الرسم البياني للاعتماديات غير المباشرة يُحلّ عند كل تثبيت من PyPI، فأي اختراق لحزمة في السلسلة يُنفَّذ بنقرة مزدوجة على `run.bat`.
3. **`pip` قد يبني من المصدر** — لو لم تتوفر عجلة جاهزة للمنصة، `setup.py` يُنفَّذ.

هذه ليست عيوبًا فادحة في أداة محلية شخصية، والمقايضة مع سهولة الاستعمال مفهومة ومقبولة. لكن السطر الواحد الناقص هو **إعلامها للمستخدم**: أضف في README تحت «التثبيت» فقرة تقول إن المشغّلات تثبّت تلقائيًا على مستوى النظام، وأن من يفضّل العزل يستعمل:

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
```

### 4.6 تصلّب CI — 🟡

`.github/workflows/tests.yml` نظيف وبسيط ويشغّل على مصفوفة بايثون. ملاحظتان:

1. **لا كتلة `permissions:`** — يعني `GITHUB_TOKEN` يستلم صلاحيات المستودع الافتراضية، وهي في كثير من المستودعات `read/write` على المحتويات. والـworkflow يشتغل `on: pull_request` أي **من التفرّعات (forks) أيضًا**. الـworkflow نفسه لا يستعمل الرمز إطلاقًا، فالصلاحية الزائدة مجانية بالكامل. أضف سطرين:

```yaml
permissions:
  contents: read
```

2. **الإجراءات مثبَّتة على وسم رئيسي** (`actions/checkout@v4`, `actions/setup-python@v5`) لا على SHA. مقبول لإجراءات رسمية من GitHub، ويستحق الذكر فقط.

3. **CI لا يشغّل أي فحص أمني** — لا `pip-audit` ولا `safety`. خطوة واحدة تكشف CVE في الاعتماديات تلقائيًا:

```yaml
- run: pip install pip-audit && pip-audit --strict || true
```

### 4.7 كتابة الملفات ومسارات الإخراج — سليم ✓

اسم المخرَج مشتقّ دائمًا من `os.path.basename()` للمدخل، فلا اجتياز مسار (path traversal) من محتوى الملف. الكتابة فوق ملف موجود ممنوعة افتراضيًا في الطرفين: `--force` مطلوبة في CLI (ومُختبَرة)، وتأكيد صريح يعرض أسماء الملفات في الواجهة. الترميز `utf-8` صريح في كل عمليات الكتابة. والافتراض الآمن (تخطّي الموجود) هو الافتراضي، والخطر يحتاج قرارًا واعيًا — وهذا هو الترتيب الصحيح.

### درجة القسم: **8/10**

لا ثغرات حرجة، ولا شيء يمكن استغلاله عن بُعد. الملاحظتان اللتان تستحقان العمل فعلًا هما **أرضية `PyMuPDF` المنخفضة** و**هجرة `fitz` → `pymupdf`**، والباقي تصلّب اختياري. لأداة تحلّل ملفات غير موثوقة، ٨/١٠ درجة عادلة: الأساسيات مضبوطة والفجوة الوحيدة الحقيقية في إدارة الاعتماديات.

---

## 5. الدرجة الإجمالية المرجّحة

| الناحية | الدرجة | الوزن | المساهمة |
|---|---|---|---|
| تقنية وهندسية | 7.8/10 | 45% | 3.51 |
| UX/UI | 7.8/10 | 30% | 2.34 |
| أمن سيبراني | 8.0/10 | 25% | 2.00 |
| **الإجمالي** | | | **7.85/10** |

**الحساب صراحةً:** (7.8 × 0.45) + (7.8 × 0.30) + (8.0 × 0.25) = 3.51 + 2.34 + 2.00 = **7.85 ≈ 7.9/10**

**مقارنة بالمراجعة السابقة (7.5/10):** ارتفاع ٠٫٤ نقطة. المكسب جاء من الاختبارات (2 → 6) وإزالة التكرار (7 → 8.5)، وابتلعه جزئيًا تشديدُ الحكم في معالجة الأخطاء (6 → 5.5) بعد أن أثبت التشغيل الفعلي انهيارًا لم يُرصد بالقراءة وحدها، وفي إمكانية الوصول (5.5 → 6 مع كشف فجوات أوضح).

**الحكم بصراحة:** المشروع **يحلّ مشكلة حقيقية لم تُحلّ جيدًا من قبل، بمنهج هندسي أنظف مما تفعله أدوات تجارية**، وتوثيقه ومعماريته أعلى بكثير من المتوسط. ما يمنعه من الثمانيات العليا ليس البنية — البنية سليمة — بل **ثلاث حالات حدّية غير معالَجة تظهر عند أول استعمال جدّي** (انهيار الكتابة، انقلاب نمط `auto` على المستندات المجدوَلة، مراسي الفهرس المكسورة)، و**فجوة اختبار في النمط الرئيسي المعلَن نفسه**. كلها إصلاحات محدَّدة النطاق، لا إعادة كتابة. بعد بنود «حرج» و«مهم» في الجدول أدناه، المشروع يستحق ٨٫٥ فما فوق بجدارة.

---

## 6. خطة الإصلاح المرتبة بالأولوية

| # | الأولوية | الناحية | المشكلة | الحل المقترح | التأثير المتوقع بعد الحل |
|---|---|---|---|---|---|
| 1 | 🔴 حرج | هندسة | `cli.main()` — الكتابة `open(out, "w")` خارج `try/except`، فمسار مخرَج غير موجود أو قرص ممتلئ ينهي الدفعة بـtraceback خام بعد إتمام التحويل (مؤكَّد بالتشغيل: `FileNotFoundError`) | انقل `open`/`write` داخل نفس `try`، وسبقهما بـ`os.makedirs(os.path.dirname(os.path.abspath(out)), exist_ok=True)` | وعد «الدفعة لا تسقط» يصير صحيحًا فعلًا، والفشل يظهر كسطر خطأ عربي بدل انهيار |
| 2 | 🔴 حرج | هندسة | `structure.body_font_size()` — الترجيح بعدد الحروف ينقلب على صفحة فيها نص صغير كثيف (جداول/حواشي)، فيصير المتن الحقيقي **عناوين** والجدول متنًا (مؤكَّد بالتشغيل) | اسقف مساهمة السطر: `weight[...] += min(len(l["text"]), 80)`، وأضف حارسًا: لو الحجم المختار أصغر من وسيط الأحجام بأكثر من نقطتين ارجع للوسيط | نمط `auto` يصمد على المستندات القانونية المجدوَلة — وهي الاستعمال المستهدَف |
| 3 | 🟠 مهم | أمن | `requirements.txt` — أرضية `PyMuPDF>=1.24` تسمح بإصدار من ٢٠٢٤ لمكتبة C تحلّل ملفات غير موثوقة، و`run.sh` لا يرقّي ما دام الاستيراد ينجح | ارفع إلى `PyMuPDF>=1.26,<2`، أضف فحص إصدار محذِّرًا في `main.py`، وأضف `.github/dependabot.yml` | إغلاق الفجوة الأمنية الوحيدة الحقيقية في المشروع |
| 4 | 🟠 مهم | أمن/هندسة | `import fitz` في `core.py`, `structure.py`, `test_cli.py`, `test_structure.py` — مهجور معلَن في PyMuPDF 1.28 وسيُزال داخل خط `1.x`، و`<2` لا يحمي. و`main.py` يفحص باسم `fitz` فسيعلن «متطلبات ناقصة» زورًا | `try: import pymupdf as fitz / except ImportError: import fitz` في الأربعة، وبدّل `CORE_DEPS` إلى `("pymupdf", "PyMuPDF")` بنفس الاحتياط | الأداة تنجو من ترقية PyMuPDF القادمة بدل أن تتوقف كليًا |
| 5 | 🟠 مهم | هندسة | صفر اختبارات لنمط `saudi_law` — وهو النمط الرئيسي المعلَن في README وفي أمثلة CLI (٥ تعابير نمطية + فرع `classify` + حالة `in_laiha` بلا حارس) | `tests/test_saudi_law.py`: الباب والفصل عنوانين علويين، المادة عنوانًا فرعيًا، «اللائحة التنفيذية» تسبق المادة فتلتحم بعنوانها، والبنود `3/1-` تُحفظ بترقيمها | أخطر فرع منطقي في المشروع يصير محروسًا؛ CI يتوقف عن الخضرة الكاذبة |
| 6 | 🟠 مهم | هندسة | `structure.anchor_of()` — بلا تصغير حروف (فـ`#Chapter-One` رابط ميت على GitHub) وبلا فضّ تعارض (عنوانان متطابقان ← مرساة واحدة، والثاني غير قابل للوصول). مؤكَّد بالتشغيل | أضف `.lower()`، ومرّر قاموس `seen` أثناء بناء الفهرس يلحق `-2`, `-3` عند التكرار | فهرس الملفات الطويلة يشتغل فعلًا — والمستندات القانونية مليئة بعناوين متكررة |
| 7 | 🟠 مهم | هندسة | `common.out_path_for()` تنشئ مجلدات كأثر جانبي، و`gui.run_convert()` تستدعيها لمجرد الفحص — فتُنشأ المجلدات قبل موافقة المستخدم، وفشل `makedirs` داخل slot يوقع PyQt6 بـ`qFatal` | احذف `makedirs` من الدالة (تصير حسابية بحتة)، وانقلها لموضع الكتابة، ولفّ `run_convert()` بـ`try/except` يعرض `QMessageBox.critical` | لا آثار جانبية على القرص من دالة حسابية، ولا انهيار صامت للواجهة |
| 8 | 🟠 مهم | هندسة/UX | `structure.convert()` — نطاق صفحات خارج المدى (`--pages 500-600` على ملف من صفحتين) يتحوّل بصمت إلى «الملف كله»، والاختبار يثبّت السلوك كأنه ميزة | افصل الحالتين: `lo >= n` ← `ValueError` صريح؛ النطاق المقلوب ← قصّ مع رسالة `say()` تحذيرية. وحدّث الاختبار ليتوقع التحذير | المستخدم يعرف أنه أخطأ بدل أن يستلم ٦٠٠ صفحة ظنًّا أنها ١٢٠ |
| 9 | 🟠 مهم | UX | لا حفظ لأي إعداد — مستخدم الأنظمة السعودية يعيد اختيار `saudi_law` ومجلد الحفظ في **كل** تشغيل | `QSettings("PDF2MD", "PDF2MD")` — احفظ في `closeEvent` واقرأ في `_build()` (~١٥ سطرًا) | إزالة أكبر احتكاك يومي في التطبيق |
| 10 | 🟠 مهم | هندسة | `core.build_lines()` يحسب `is_row` و`CELL_GAP` لكل سطر — و**لا أحد يقرأهما في المستودع كله**. وREADME يوثّق `CELL_GAP` كأنه فعّال | إما استهلك `row=True` في `structure.convert()` لبناء جدول Markdown بـ`\|`، أو احذف `is_row` و`CELL_GAP` من الكود ومن جدول README | إمّا ميزة الجداول تكتمل، وإمّا يختفي كود ميت يوهم القارئ بميزة غير موجودة |
| 11 | 🟠 مهم | UX/وصول | لا `setBuddy` ولا `setAccessibleName` في `gui.py` إطلاقًا، ولا `:focus` في `QSS` — التنقّل بلوحة المفاتيح يفقد موضعه بين ستة مربّعات اختيار | `label.setBuddy(widget)` لكل زوج في `_options_box`، و`setAccessibleName` للعناصر الرئيسية، وقاعدة `:focus { border: 2px solid GOLD; }` | الواجهة تصير صالحة لقارئ الشاشة وللتنقّل بلوحة المفاتيح |
| 12 | 🟠 مهم | أداء/UX | `diagnose()` يشغّل فحص الحبر افتراضيًا (١٢٩ عملية رسم على كتاب ٩٠٠ صفحة) ولا يقبل `cancel`، والواجهة تعطّل زر الإيقاف أثناءه | مرّر `check_ink=False` داخل `diagnose` (لا علاقة له بقياس الرباطات)، وأضف معامل `cancel` ومرّره من `DiagWorker`، وفعّل زر الإيقاف | «الفحص السريع» يصير سريعًا فعلًا وقابلًا للإيقاف |
| 13 | 🟡 تحسين | هندسة | مفتاح تطبيع الترويسة `re.sub(r"\d+", "#", ...)` مكرر ٣ مرات نصًّا بين `repeated_headers()` و`convert()` — أي تعديل في موضع واحد يعطّل حذف الترويسات بصمت | دالة واحدة `boiler_key(text)` في `structure.py` واستدعِها في الثلاثة (ورقِّها لتشمل `[\d٠-٩]+`) | استحالة اختلال التزامن بين بناء المجموعة والبحث فيها |
| 14 | 🟡 تحسين | أمن | `.github/workflows/tests.yml` بلا كتلة `permissions:` رغم أنه يشتغل `on: pull_request` من التفرّعات ولا يستعمل الرمز أصلًا | أضف `permissions:\n  contents: read`، وخطوة `pip-audit` اختيارية | صلاحية زائدة مجانية تُغلَق بسطرين |
| 15 | 🟡 تحسين | أمن | `QMessageBox` بـ`AutoText` يصيّر أسماء الملفات كـHTML (`<b>x</b>.pdf` يظهر عريضًا) في `next_job()` و`run_convert()` | ابنِ `QMessageBox` صراحةً مع `setTextFormat(Qt.TextFormat.PlainText)` | إغلاق آخر مسار لتصيير نص خارجي في الواجهة |
| 16 | 🟡 تحسين | هندسة | لا `ruff`/`black`/`.editorconfig`، وCI يشغّل `pytest` فقط. و`l` كاسم متغيّر (`E741`) و`B = Builder(opt)` يخالفان PEP 8 | بدّل `l` → `line` و`B` → `builder`، أضف `[tool.ruff]` في `pyproject.toml` وخطوة `ruff check .` في CI | الاتساق الحالي (الممتاز) يصير محروسًا آليًا لا معتمدًا على شخص |
| 17 | 🟡 تحسين | UX | شريط التقدّم لكل ملف لا للدفعة — ١٠ ملفات = الشريط يعيد ٠→١٠٠ عشر مرات بلا مؤشّر «٣ من ١٠»، والمعاينة تُستبدَل بصمت | احسب النسبة الكلية `int((done * 100 + pct) / total)` واعرض `[3/10]` في شريط الحالة، وأضف اسم الملف في رأس المعاينة | المستخدم يعرف أين هو في الدفعة الطويلة |
| 18 | 🟡 تحسين | UX | `preview.setPlainText(md[:200000])` يقطع بصمت — المستخدم يظن الناتج ناقصًا | ألحق سطرًا: `… [المعاينة مقطوعة عند 200,000 حرف — الملف المحفوظ كامل (N حرفًا)]` | إزالة قلق لا داعي له على الكتب الكبيرة |
| 19 | 🟡 تحسين | UX | زر «تفريغ» يمسح ٤٠ ملفًا بنقرة بلا تأكيد ولا تراجع، بينما التطبيق يؤكّد للكتابة فوق الملفات | `QMessageBox.question` قبل التفريغ لو `count() > 3` | اتساق سياسة التأكيد داخل التطبيق نفسه |
| 20 | 🟡 تحسين | UX/بصري | نظاما ألوان في شاشة واحدة: `Qt.GlobalColor.red` في `show_diag()` بجانب `BAD = "#B3261E"` المستعمل في الدالة نفسها. وتعارض `QFont("Amiri", 26)` مع `font-size: 30px` في QSS | `item.setForeground(QColor(BAD))` و`QColor(OK)`، واحذف إحدى قاعدتي حجم العنوان | لوحة ألوان واحدة فعلًا، لا اثنتان |
| 21 | 🟡 تحسين | UX/وصول | `QPushButton:disabled` تباينه **1.66:1** — نص الزر يكاد يختفي أثناء التحويل | ارفع لون النص المعطَّل إلى `#6F6355` (~3.1:1) — يبقى واضحًا أنه معطَّل ويبقى مقروءًا | حالة «جارٍ التحويل» تظل مفهومة بصريًا |
| 22 | 🟡 تحسين | توثيق | README يقول «بلا أي تلاعب بـ `sys.path`» و`main.py` يسوّي `sys.path.insert` بالضبط؛ و«الحدود المعروفة» الإنجليزية تنقص بندين موجودين بالعربي؛ و`conftest.py` فارغ صفر بايت بلا تعليق فيُحذَف ظنًّا أنه بقايا | صحّح الجملة، وزامن قائمتي الحدود، واكتب سطرين في `conftest.py` يشرحان لماذا يجب ألا يُحذف | إغلاق آخر التناقضات في توثيق ممتاز أصلًا |
| 23 | 🟡 تحسين | أمن/توثيق | `run.sh`/`run.bat` يثبّتان تلقائيًا على مستوى النظام بلا venv وبلا `--require-hashes` — قرار تجربة مستخدم مفهوم، لكنه غير مذكور للمستخدم | فقرة في README تحت «التثبيت» تذكر ذلك وتعرض بديل `python -m venv .venv` لمن يفضّل العزل | المستخدم يقرّر بوعي بدل أن يُفاجأ |
| 24 | 🟡 تحسين | هندسة | لا `pyproject.toml` — لا `pip install .` ولا توزيع ولا venv نظيف، وكل التشغيل معتمد على الوقوف في جذر المشروع | `pyproject.toml` بسيط (`[project]` + `[tool.ruff]` + `[tool.pytest.ini_options]`) و`console_scripts` باسم `pdf2md` | المشروع يصير قابلًا للتثبيت والتوزيع، وأمر التشغيل يصير `pdf2md file.pdf` |

---

## ملحق — ما تحقّقت منه بالتشغيل لا بالقراءة

| الادعاء | كيف أُثبت | النتيجة |
|---|---|---|
| الاختبارات تعبر | `pytest -q` | ✅ ٢٤/٢٤ في ٠٫٣٥ ث |
| فحص الحبر يبطئ ٣× كما يعد README | تحويل ملف ٢٠ صفحة بالخيارين | ✅ ٠٫٠١ ث ← ٠٫٠٣ ث |
| مسار مخرَج غير موجود ينهي CLI | `-o /tmp/nodir/x.md` | ❌ `FileNotFoundError` خام |
| نطاق خارج المدى يُرفض | `page_from=500, page_to=600` على ملف من صفحتين | ❌ حوّل الصفحتين بصمت |
| `out_path_for` حسابية بلا أثر | استدعاء مفرد ثم `os.path.isdir` | ❌ أنشأت المجلد |
| مراسي الفهرس تعمل | تحويل ملف بعنوانين متطابقين | ❌ `#Chapter-One` ×٢ (بلا تصغير، بلا فضّ تعارض) |
| نمط `auto` يصمد مع نص صغير كثيف | صفحة متن 12pt + جدول 9pt | ❌ المتن صار عناوين والجدول متنًا |
| `is_row` / `CELL_GAP` مستهلَكان | بحث في المستودع كله | ❌ صفر قارئ — كود ميت |
| `import fitz` سليم مستقبلًا | تشغيل على PyMuPDF 1.28.2 | ⚠ تحذير إهمال معلَن بالإزالة |
| لا أسرار في الكود أو التاريخ | مسح ١١ commit + الكود كله | ✅ نظيف |
| الهروب من HTML في التشخيص | قراءة `show_diag` | ✅ `producer`/`creator`/`pairs` مهرَّبة |
| لا `:focus` ولا أسماء وصول | بحث في `gui.py` | ❌ صفر ورود للثلاثة |
