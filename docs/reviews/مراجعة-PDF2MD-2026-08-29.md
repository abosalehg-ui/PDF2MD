# مراجعة مِحَك — PDF2MD

> **التاريخ:** ٢٠٢٦/٠٨/٢٩ · **الفرع:** `claude/mihak-skill-4jjybt` · **الالتزام:** `3579d18`
> **المراجعة الرابعة** — بعد `مراجعة-PDF2MD.md` (7.5) و`مراجعة-PDF2MD-2026-08-16.md` (7.9) و`مراجعة-PDF2MD-2026-08-18.md` (7.8).
>
> **منهج هذه المراجعة:** كل رقم فيها **مقيس لا مقدَّر**. ثُبّتت الاعتماديات فعليًا، وشُغّلت الاختبارات الـ١٨٨ كلها (ومعها اختبارات الواجهة الرسومية بقناة `offscreen` بعد تثبيت مكتبات Qt النظامية)، وقيست التغطية بـ`coverage`، وشُغّل `ruff` و`pip-audit`، ووُلّدت ملفات PDF اصطناعية لإثبات العيوب الحدّية، وحُسبت نِسَب تباين اللوحتين (الفاتحة والداكنة) رياضيًا بمعادلة WCAG، و**نُفّذ `web/js/engine.js` فعلًا على Node** بخيط عامل وهمي لإثبات مسارَي التجمّد.
>
> نطاق المراجعة: **المستودع كامل** — كل ملف مصدر قُرئ كاملًا، ولم تُستثنَ عيّنة.

---

## ✅ حالة التنفيذ

**نُفّذت خطة الإصلاح كاملة — البنود السبعة عشر — في الالتزامين التاليين لهذا التقرير.**

| البند | الحالة | الأثر المقيس بعد التنفيذ |
|---|---|---|
| ١ — `_body_chars` تقرأ `text` غير الموجود | ✅ | العدّ على `chars`: **٠ ← ١٣٢٠** حرفًا على الصفحة نفسها، والحكم صار `(False, "")` |
| ٢ — مسارا تجمّد `engine.js` | ✅ | `shutdown()` ومهلة صمت؛ واختباران يثبتان الرفض بدل التعليق |
| ٣ + ١٥ — ضريبة حكم الـOCR وتكرار التحليل | ✅ | `auto` **٠٫٤٢ ← ٠٫٢٤ ث** على ٤٠ صفحة — الضريبة زالت بالكامل |
| ٤ — تباعد وصف الياء المكسورة | ✅ | `broken_yeh_hits` تحتكم إلى `core.is_yeh_pair` |
| ٥ + ١٢ — ثغرات الاختبار | ✅ | **١٩٩ اختبار بايثون + ١٠ جافاسكربت**؛ `ocr.py` **٥٥٪ ← ٦٥٪**، والإجمالي **٨١٪ ← ٨٢٪** |
| ٦ — ثلاث مخالفات تباين | ✅ | ٤٫٢٢ ← **٥٫٠١** · ٢٫٧٣ ← **٣٫٣١** · ٢٫٣٨ ← **٣٫٧٣** |
| ٧ + ١١ — فيضان الجوال وهدف اللمس | ✅ | الصفّ يلتفّ والاسم يُقصّ؛ زر الحذف **٢٨×٢٨** |
| ٨ + ١٠ + ١٣ + ١٦ — تجربة الاستعمال | ✅ | تصفير النتائج، وشكل أرقام موحّد، وتنبيه الحجم، وتنقّل الألسنة بالأسهم |
| ٩ + ١٧ — ترويسات ومهلة شبكة | ✅ | `frame-ancestors` و`COOP`/`COEP` في `vercel.json`، ومهلة ٦٠ ث للتنزيل |
| ١٤ — قسمة `gui.py` | ✅ | **٩٩١ ← ٥٧١** سطرًا، ومعها `gui_panels` و`gui_theme` و`gui_workers` |

> **تصحيح قياس:** رقم «+٧٥٪» المذكور في البند ٢٫٥ أدناه كان مقيسًا على تشغيلة باردة.
> القياس الدقيق (أفضل خمس تشغيلات بعد إحماء) هو **+٦٨٪**: `never` ٠٫٢٥ ث مقابل
> `auto` ٠٫٤٢ ث. والخلاصة لم تتغيّر، والضريبة زالت بالكامل بعد الإصلاح.

ما تحت هذا السطر هو التقرير كما كُتب عند المراجعة — يبقى سجلًّا لحالة المستودع
عند الالتزام `3579d18`، لا يُعدَّل بعد التنفيذ.

---

## 1. نظرة عامة سريعة

**PDF2MD** أداة تستخرج نصًّا عربيًّا سليمًا من ملفات PDF المصدَّرة من Word ثم المعالَجة عبر محرّك Quartz في macOS/iOS — وهي ملفات تحمل خللًا في خريطة `ToUnicode` يجعل الرباطات العربية تُخزَّن معكوسة (`املادة` بدل `المادة`) — وتحوّلها إلى Markdown منظَّم بعناوين وفقرات وجداول وحواشٍ وفهرس.

البنية أربع طبقات نظيفة تحتها **ثلاث واجهات**: `core.py` محرّك استخراج هندسي بحت (إحداثيات + بكسلات، بلا قاموس ولا تخمين لغوي)، و`structure.py` طبقة البنية والتصنيف، و`ocr.py` رصد الطبقة المعطوبة والسقوط إلى Tesseract، و`common.py` المشترك — وفوقها `cli.py` و`gui.py` (PyQt6) و`web.py` الذي يعمل حرفيًا بلا تعديل داخل المتصفّح فوق Pyodide.

الجديد منذ المراجعة الثالثة: **واجهة الويب كاملة** (صفحة ساكنة ذاتية الاستضافة بلا خادم تحويل)، و**مسار OCR** كامل، ونشر مزدوج على GitHub Pages و Vercel.

| البند | القيمة |
|---|---|
| التقنيات | Python ≥ 3.9 · PyMuPDF 1.28.2 · numpy ≥ 1.24 · PyQt6 ≥ 6.6 (اختياري) · Pyodide 0.28.3 · Tesseract (اختياري) |
| مصدر بايثون | ٧ ملفات · **٣٣٤٢ سطرًا** (`gui.py` وحده ٩٨٨) |
| واجهة الويب | `index.html` + ٤ ملفات JS + CSS · **١٨٦١ سطرًا** |
| أدوات البناء | ٣ ملفات · ٢٥٨ سطرًا |
| الاختبارات | ١١ ملفًا · **١٨٨ اختبارًا · كلها ناجحة في ٢٫٣٠ ثانية** |
| التغطية المقيسة | **٨١٪** على `src/` — و`ocr.py` **٥٥٪** وهي الأدنى |
| `ruff check .` | **All checks passed** |
| `pip-audit` | **No known vulnerabilities found** |
| CI | ٤ وظائف: اختبارات (٣٫٩ و٣٫١٢) + واجهة رسومية `offscreen` + `ruff` + `pip-audit` (تنبيهي) |
| README | ٦٧٦ سطرًا — عربي/إنجليزي، يشرح كل قاعدة هندسية برقمها المقيس |

---

## 2. المراجعة التقنية والهندسية (45%)

### ٢٫١ البنية المعمارية وفصل المسؤوليات — **9/10**

الفصل ممتاز ونادر في مشاريع بهذا الحجم. `core.py` لا يعرف شيئًا عن Markdown، و`structure.py` لا يعرف شيئًا عن الواجهة، والواجهات الثلاث تعيش فوقهما بلا أن تعرف بعضها. وأقوى قرار معماري في المشروع كله أن **صفحة الويب تشغّل ملفات `src/*.py` نفسها بلا نسخة ثانية**:

> `web/runtime.json` — مصدر الحقيقة الوحيد:
> ```json
> "sources": ["__init__.py", "common.py", "core.py", "ocr.py", "structure.py", "web.py"]
> ```
> يقرؤه `worker.js` عند الإقلاع و`tools/build_site.py` عند النشر — فلا تنشأ قائمتان تتباعدان.

**الخصم الوحيد** تسريب طبقي حقيقي: ظاهرة «الياء المكسورة» موصوفة **مرّتين** في وحدتين — مرة في `core._is_yeh_pair()` (المُصلِح) ومرة في `ocr.broken_yeh_hits()` (الراصد) — وقد **تباعد الوصفان فعلًا**، وهذا موثّق بالتشغيل في البند ٢٫٣ أدناه.

**الحل:** ارفع الرصد فوق المُصلِح: اجعل `ocr.broken_yeh_hits()` تستدعي `core._is_yeh_pair()` بدل أن تعيد كتابة شروطها.

---

### ٢٫٢ قابلية القراءة والصيانة — **8.5/10**

التعليقات في هذا المشروع من أفضل ما يُقرأ: تشرح **«ليش»** لا «وش»، ومعها الرقم المقيس والحالة التي دفعت إليها. مثال من `core.py`:

```python
# القياس الفعلي: المسافة الحقيقية <= ٢٪ حبر، والوهمية ٩–٢١٪.
# فالعتبة INK_MAX = ٦٪ تفصل بينهما بهامش واسع من الجهتين.
```

وحتى `conftest.py` الفارغ يحمل تعليقًا يشرح لماذا يجب ألّا يُحذف. هذا مستوى توثيق داخلي يتفوّق على أغلب المشاريع التجارية.

**ما يشدّ للأسفل:**

`src/gui.py` — ٩٨٨ سطرًا، ٣٠٪ من مصدر بايثون في ملف واحد. الفئة `MainWindow` وحدها تحمل بناء الواجهة، وإدارة الطابور، والحوارات، والإعدادات، والتشخيص، والتحويل. و`_options_box()` دالّة بناء واحدة طولها ~١٠٠ سطر تبني أحد عشر عنصر تحكم بمتغيّر عدّاد `r` يُزاد يدويًا اثنتي عشرة مرة.

**الحل:** اقسم `MainWindow` إلى `MainWindow` + `OptionsPanel` (QWidget مستقل يملك `options()` و`_checkboxes()`) + `BatchRunner` (يملك `queue` و`_workers` و`next_job`). القسمة الأولى وحدها تنزل بالملف تحت ٦٠٠ سطر.

---

### ٢٫٣ التكرار (DRY) و Code Smells — **7.5/10**

هنا يظهر ثمن التكرار **بعيبٍ حقيقي لا نظري**. الظاهرة الواحدة موصوفة في موضعين، والوصفان انحرفا في اتجاهين متعاكسين:

> `src/core.py` — في `_is_yeh_pair()` (المُصلِح):
> ```python
> YEH_DOTS = frozenset("ًٌ")        # تنوين الفتح **و** الضم
> ...
> x0, x1 = base["bbox"][0], base["bbox"][2]
> return x0 <= dots["bbox"][0] < x0 + (x1 - x0) * YEH_DOTS_SIDE   # شرط الموضع
> ```
>
> `src/ocr.py` — في `broken_yeh_hits()` (الراصد):
> ```python
> if chars[i]["c"] != "ٌ":                     # تنوين الضم **وحده**
>     continue
> ...                                          # ولا شرط موضع إطلاقًا
> ```

**مقيس بالتشغيل الفعلي:**

| الحالة | `core` (المُصلِح) | `ocr` (الراصد) | النتيجة |
|---|---|---|---|
| ياء مكسورة بتنوين **الفتح** `U+064B` | يُصلحها → `ي` | **٠ شواهد** | سلبية كاذبة — الملف المعطوب لا يُرسَل إلى OCR |
| ياء مكسورة بتنوين **الضم** `U+064C` | يُصلحها → `ي` | ١ شاهد | متّفقان ✓ |
| **تشكيل طائر حقيقي** (التنوين عند الحافة المقابلة) | `_is_yeh_pair` = **False** — «ليس ياءً» | **١ شاهد** | إيجابية كاذبة |

الإيجابية الكاذبة هي الأخطر: `BROKEN_YEH_HITS = 3` فقط، فثلاث كلمات تنتهي بـ«ـًا» في صفحة واحدة تكفي للحكم بأن «خريطة الخط مبدَّلة» و**إرسال الصفحة السليمة كلها إلى OCR** — أي استبدال طبقة نص ممتازة بقراءة ضوئية أدنى، وهو عكس ما يعد به `ocr.py` في وثيقته: «الصفحة السليمة تبقى على نصها الأصلي — فهو أدقّ من أي OCR».

**تكرار ثانٍ:** بناء `Options` مكرر في **ثلاثة** مواضع الآن (`cli.options_from` و`gui.options` و`web.build_options`). المراجعة الثالثة أوصت بحارس، وقد نُفّذ — لكن **للواجهة الرسومية وحدها**:

```python
# tests/test_gui.py — الحارس موجود
assert boolean_fields == set(window._checkboxes())
```
ولا نظير له لـ`web.BOOL_FIELDS` في `tests/test_web.py`. المطابقة اليوم صحيحة (مقيسة: الثمانية = الثمانية)، لكن خيارًا منطقيًا جديدًا سيغيب عن الويب بصمت — وهو بالضبط العطل الذي كُتب حارس الواجهة الرسومية لمنعه.

**الحل:** انسخ اختبار التكافؤ إلى `tests/test_web.py` مقابل `web.BOOL_FIELDS`؛ ثلاثة أسطر تُغلق الثغرة الثالثة.

---

### ٢٫٤ معالجة الأخطاء والحالات الحدّية — **7/10**

**جانب بايثون ممتاز:** فشل ملف واحد لا يُسقط الدفعة (`cli.main`)، والملف المحمي يُرفض برسالة عربية، والنطاق المقلوب يُرفض في الواجهتين، و`QSettings` التالفة تسقط إلى الافتراضي بدل منع الإقلاع (`gui._num`)، وحتى فشل حذف الملف المؤقت في `worker.js` لا يُسقط المهمة.

**جانب جافاسكربت فيه عيبان أُثبتا بالتنفيذ الفعلي** — كلاهما يُجمّد الواجهة تجمّدًا دائمًا لا مخرج منه إلا تحديث الصفحة:

> **(أ) الإقلاع البديل الفاشل بعد «إيقاف»** — `web/js/engine.js`:
> ```js
> #crash(message) {
>   const error = new Error(message);
>   if (this.bootReject && !this.about) this.bootReject(error);   // ← الحارس
> ```
> `cancel()` تصفّر `worker` و`ready` و`pending`، لكنها **لا تصفّر `about`**. فإن فشل الخيط البديل في الإقلاع، يبقى `about` محمّلًا من الجلسة الأولى، فيُبطل الشرط `!this.about` رفضَ الوعد، ويبقى `ready` معلّقًا إلى الأبد.
>
> **مُثبَت بالتنفيذ على Node:**
> ```
> الإقلاع الأول    : نجح، about = {"version":"2.0.0"}
> بعد فشل الإقلاع الثاني: engine.about = {"version":"2.0.0"} → الواجهة تحسبه جاهزًا: true
> نتيجة استدعاء تحويل جديد: HANG — الوعد لم يُحسم أبدًا
> ```

> **(ب) الخيط الميت يبقى مرجَّعًا بعد انهيار قاتل** — نفس الملف: `#crash` ترفض المهمة الجارية لكنها **لا تتخلّص من الخيط ولا تُبطل `ready`**. فالمهمة التالية تُرسَل إلى خيط ميت، ولا تعود منه رسالة أبدًا.
>
> **مُثبَت بالتنفيذ على Node:**
> ```
> المهمة الأولى : رُفضت: نفاد ذاكرة في الخيط العامل
> بعد الانهيار  : worker = ما زال مرجَّعًا (ميت) | ready = ما زال محسومًا
> المهمة الثانية: HANG — لا رفض ولا نتيجة، الواجهة تتجمّد
> ```
> والمسار إلى هذا الانهيار هو بالضبط ما يحذّر منه README نفسه: «لسان المتصفّح محدود الذاكرة… الملفات الضخمة جدًا قد تفشل هنا».

**الحل (سطران):**
```js
cancel() { ...; this.about = null; ... }          // (أ)

#crash(message) {                                  // (ب)
  const error = new Error(message);
  if (this.worker) { this.worker.terminate(); this.worker = null; }
  this.ready = null;
  if (this.bootReject) this.bootReject(error);
  ...
}
```
وأضف مهلة حراسة (`watchdog`) على كل مهمة: رسالة تقدّم لا تصل خلال ٦٠ ثانية تعني خيطًا مفقودًا لا عملية بطيئة.

---

### ٢٫٥ الأداء واستهلاك الموارد — **6.5/10**

الحمايات الموجودة ممتازة ومقصودة: سقف `INK_MAX_PIXELS = 40M` يمنع نفاد الذاكرة من ملف بحجم كيلوبايت، و`PREVIEW_LIMIT` يحمي عنصر العرض، و`engine.js` ينقل ملكية `ArrayBuffer` بدل نسخه، و`LOG_LIMIT` يسقّف السجل.

**لكن مسار OCR الافتراضي يفرض ضريبة على كل تحويل، وتُدفَع حتى حين لا يمكن أن تُشترى بها فائدة.**

`structure._extract_pages()` يحسب `ready` (هل Tesseract متاح؟) ثم **يتجاهله عند اتخاذ القرار**:

```python
ready = opt.ocr != "never" and ocr_engine.available()
...
if opt.ocr != "never":
    if opt.ocr == "always": want, why = True, "بطلب صريح"
    else: want, why = ocr_engine.page_verdict(page)     # ← تُستدعى دائمًا
    if want and not ready:                              # ← ثم يُكتشف أنه غير متاح
        st["ocr_missed"] += 1
```

و`page_verdict()` وحدها تقرأ الصفحة **ثلاث إلى أربع مرات**: `get_text()` ثم `rawdict` في `_body_chars()` ثم `rawdict` مرة أخرى في `broken_yeh_hits()` ثم `dict` في `_image_cover()` — كل هذا **فوق** قراءة `rawdict` التي يقوم بها `core.page_lines()` أصلًا.

**القياس الفعلي** (مستند من ٤٠ صفحة متن عربي، بلا Tesseract مثبَّت على هذا الجهاز):

| الوضع | `--no-ink` | مع فحص الحبر |
|---|---|---|
| `--ocr never` | **٠٫٤٤ ث** | **٠٫٥٧ ث** |
| `--ocr auto` (الافتراضي) | **٠٫٧٧ ث** (+٧٥٪) | **٠٫٩٠ ث** (+٥٨٪) |

أي أن المستخدم الذي لا يملك Tesseract — وهو **الحالة الافتراضية لأغلب المستخدمين، و«دائمًا» في المتصفّح** — يدفع ٧٥٪ من زمن التحويل ثمنًا لقرارٍ لا يمكن تنفيذه أصلًا.

**الحل:**
1. في `_extract_pages`: `if ready or opt.ocr == "always":` قبل استدعاء `page_verdict` — واحتفظ بتحذير واحد للمستخدم أن OCR معطَّل لغياب Tesseract.
2. في `web.build_options()`: `opt.ocr = "never"` صراحةً — لا Tesseract في Pyodide بحال، فالحساب هناك ضائع بالكامل.
3. مرحلة ثانية: مرّر مخرَج `rawdict` **مرة واحدة** إلى `page_verdict` و`page_lines` معًا بدل إعادة تحليل الصفحة لكل فحص.

---

### ٢٫٦ الاختبارات — **8/10**

١٨٨ اختبارًا كلها ناجحة في ٢٫٣٠ ثانية، وتغطية ٨١٪. والأهم من الرقم **نوعية** الاختبارات: اختبارات تكامل تبني ملفات PDF حقيقية بـ`fitz`، واختبارات واجهة رسومية تُقلع نافذة كاملة بقناة `offscreen` وتقيس إحداثيات العناصر، ووظيفة CI مستقلة لها. وفيها اختبار نادر جدًّا يستحق الإشادة — **اختبار العقد مع المكتبة الخارجية**:

```python
RAW_KEYS = ("alpha", "color", "chars", "size", "font", "flags")

def test_rawdict_still_carries_the_keys_we_depend_on(tmp_path):
    """غياب أيٍّ منها لا يرمي استثناءً — بل يعطّل الرصد **بصمت**."""
```

**والمفارقة أن هذا الاختبار بالذات يدين العيب الحرج في البند ٢٫٧:** قائمة المفاتيح المسموح الاعتماد عليها **لا تحوي `text`** — لأن `rawdict` لا يحويه — ومع ذلك يقرؤه `ocr._body_chars()`.

**الثغرة:** `ocr.py` عند **٥٥٪** وهي الأدنى في المشروع، والأسطر غير المغطّاة هي `_body_chars` و`page_verdict` و`_image_cover` بالضبط:

```
src/ocr.py   165   74   55%   ... 211, 213, 215, 223-235, 245-273 ...
```

ملف الاختبارات `tests/test_ocr.py` يبني قواميس يدوية ولا يمرّر **أي صفحة PyMuPDF حقيقية** على `page_verdict`. لذلك مرّ عيب حرج من CI أخضر.

**الحل:** اختبار تكامل واحد يبني بـ`fitz` صفحة نصية كثيفة + شعارًا صوريًا، ويؤكد `page_verdict(page) == (False, "")`. سطران يمنعان تكرار البند ٢٫٧.

---

### ٢٫٧ 🔴 عيب حرج: الصفحة السليمة تُشخَّص «ممسوحة ضوئيًا بلا طبقة نص»

هذا أخطر ما في المراجعة، ويستحق قسمًا مستقلًا.

> `src/ocr.py` — في `_body_chars()`:
> ```python
> return sum(
>     not ch.isspace()
>     for blk in blocks
>     for line in blk["lines"]
>     for span in line["spans"]
>     for ch in span.get("text", "")      # ← المفتاح غير موجود في rawdict
> )
> ```

الأجزاء (spans) في `rawdict` **لا تحمل مفتاح `text` إطلاقًا** — تحمل `chars` بدلًا منه. مقيس على PyMuPDF 1.28.2 المثبَّت:

```
SPAN KEYS (rawdict): ['alpha','ascender','bbox','bidi','char_flags','chars',
                      'color','descender','flags','font','origin','size']
has 'text'?  False
SPAN KEYS (dict)   : [... 'size', 'text']      ← موجود في dict فقط
```

فـ`span.get("text", "")` ترجع سلسلة فارغة دائمًا، و**`_body_chars()` ترجع صفرًا على كل صفحة في الدنيا**:

```
حروف النص الفعلية      : 1939
_body_chars()          : 0          ← على صفحة فيها ١٩٣٩ حرفًا سليمًا
```

**ولماذا هذا حرج لا تجميلي:** الحكم في `page_verdict()` مبني على هذا الصفر:

```python
if body < MIN_CHARS:                                    # 0 < 40 → دائمًا صحيح
    cover = _image_cover(page)
    if cover >= IMG_COVER or (body == 0 and cover >= IMG_COVER_EMPTY):
        return True, "صفحة ممسوحة ضوئيًا بلا طبقة نص"
```

الفرع `body == 0` صحيح **دائمًا**، فتنهار العتبة الفعلية من `IMG_COVER = 0.50` إلى `IMG_COVER_EMPTY = 0.10`. أي أن **أي صفحة تحمل صورة تغطي ١٠٪ من مساحتها تُعدّ «ممسوحة ضوئيًا بلا نص»** مهما كان متنها غزيرًا — وشعار جهة حكومية أو ترويسة مؤسسة أو ختم في مذكرة يبلغ هذه النسبة بسهولة، وهو **الشكل الغالب للمستندات التي بُنيت الأداة لها**.

**مُثبَت بملف PDF مبني فعليًا** (٣٥ سطر متن عربي + شعار صوري يغطي ١٣٪):

```
حروف النص الفعلية      : 1914
_body_chars()          : 0
تغطية الصورة           : 0.13
page_verdict()         : (True, 'صفحة ممسوحة ضوئيًا بلا طبقة نص')

diagnose(): broken = [1] | why = صفحة ممسوحة ضوئيًا بلا طبقة نص | needs_ocr = True
```

**الأضرار الثلاثة، كلٌّ في واجهة:**

| الحال | ما يحدث |
|---|---|
| Tesseract **مثبَّت** (`--ocr auto` الافتراضي) | طبقة النص السليمة **تُرمى** وتُستبدل بقراءة ضوئية أدنى — ومعها تُفقَد كل إصلاحات `core` (الرباطات، الحبر، التشكيل) لأن ناتج OCR لا يمرّ عليها عمدًا. وREADME نفسه يحذّر: «الأرقام الهندية تُقرأ خطأً كثيرًا». **الأداة تُفسد ما جاءت لتُصلحه.** |
| Tesseract **غير مثبَّت** (سطح المكتب) | تحذير كاذب: «⚠ N صفحة طبقة نصها معطوبة ولم تُقرأ بالـOCR — ناتجها ناقص أو خاوٍ» على صفحات ناتجها سليم تمامًا |
| **المتصفّح** (لا Tesseract أبدًا) | `diagnose_file` يردّ: «طبقة النص معطوبة — حوّل الملف من سطر الأوامر أو الواجهة الرسومية» — أي أن الأداة **تطرد المستخدم** عن ملف تعالجه بامتياز |

**الحل — سطر واحد.** اقرأ `chars` كما تفعل بقية الوحدة:

```python
return sum(
    not ch["c"].isspace()
    for blk in blocks
    for line in blk["lines"]
    for span in line["spans"]
    for ch in span.get("chars") or []
)
```

**مُتحقَّق منه على نفس الملف:** `current _body_chars: 0` → `fixed _body_chars: 1530` ✓

---

### ٢٫٨ التوثيق — **9/10**

`README.md` بستمئة وستة وسبعين سطرًا عربي/إنجليزي من أفضل ما يُكتب لمشروع مفتوح: يشرح كل قاعدة هندسية بالرقم المقيس الذي وُلدت منه، وجدول ثوابت المعايرة كامل، وقسم «الحدود المعروفة» صادق لا تسويقي (يعترف بأن دقّة OCR أدنى، وأن الأرقام الهندية تُقرأ خطأً، وأن الكتب الضخمة تُحمَّل كاملة في الذاكرة)، وحتى الخطوة اليدوية في GitHub Pages موثّقة مع **سبب استحالة أتمتتها** ونصّ رسالة الخطأ التي رُدّت فعلًا.

**الخصم:** التوثيق يصف سلوكًا لا يُنفَّذ. README يقول إن رصد الصفحة الممسوحة يكون بأن «متن الصفحة (بعد حذف العلامة المائية) أقل من ٤٠ حرفًا وصورة تغطيها» — وهذا وصف دقيق للنيّة، لكن الكود لا ينفّذه (البند ٢٫٧). التوثيق هنا **أصحّ من الكود**، وهو حال نادر ومحمود، لكنه يعني أن قارئ README لن يشكّ في الرقم أبدًا.

**الحل:** بعد إصلاح ٢٫٧ يصير الوصف صحيحًا بلا تعديل. وأضف سطرًا في «الحدود المعروفة» عن سقف حجم الملف في المتصفّح.

---

### ٢٫٩ اتفاقيات التسمية ونمط موحّد — **9.5/10**

`ruff check .` نظيف بلا استثناءات مخبَّأة (الاستثناءات الثلاثة في `pyproject.toml` كلٌّ منها معلَّل بسطر). `snake_case` في بايثون و`camelCase` في جافاسكربت بلا خلط. الشرطة السفلية للدوال الداخلية متّبعة بانضباط. ترويسة `# -*- coding: utf-8 -*-` في كل ملف. وأسماء الثوابت معبّرة عن قياسها لا عن نوعها (`INK_MAX`، `WM_LUMA`، `SEG_CLASH`). لا ملاحظة تُذكر.

---

### متوسط القسم الهندسي

| النقطة | الدرجة |
|---|---|
| البنية وفصل المسؤوليات | 9.0 |
| قابلية القراءة والصيانة | 8.5 |
| التكرار و Code Smells | 7.5 |
| معالجة الأخطاء والحالات الحدّية | 7.0 |
| الأداء واستهلاك الموارد | 6.5 |
| الاختبارات | 8.0 |
| التوثيق | 9.0 |
| التسمية والنمط | 9.5 |

المتوسط الحسابي ٨٫١، ويُخفَّض إلى **7.8/10** لأن عيب `_body_chars` عيبُ **صحّة** يضرب الوظيفة الأساسية على صنف شائع جدًّا من المستندات، ولا يُنصف حسابُه بندًا واحدًا من ثمانية.

---

## 3. المظهر والتصميم وتجربة المستخدم (30%)

### ٣٫١ الاتساق البصري — **9/10**

اللوحة (بنّي `#6B4423` + ذهبي `#C9A227` على كريمي `#FAF6EE`) منقولة حرفيًا من `QSS` في `gui.py` إلى متغيّرات CSS في `styles.css`، فمن استعمل تطبيق سطح المكتب يتعرّف على الصفحة قبل أن يقرأ عنوانها. ولا لون واحد مبعثرًا خارج المتغيّرات إلا ألوان الحالة المعطَّلة. والمظهر الداكن يقلب الأدوار لا الهوية (بنّي فاتح على بنّي قاتم، لا رمادي محايد) — قرار مقصود ومعلَّل في التعليق.

نقطة دقيقة محسوبة لصالحهم: الأيقونات SVG مرسومة لا رموز تعبيرية، «لأن الرمز التعبيري يأتي بقياس خطّ النظام… ولا يقبل `color`». هذا مستوى انتباه بصري عالٍ.

### ٣٫٢ سهولة الاستخدام والتنقّل — **9/10**

المهمة الأساسية نقرتان: أفلت الملف → «تحويل». والخيارات كلها ظاهرة بلا قوائم مخفية، وتُحفظ (`localStorage` في الويب و`QSettings` في سطح المكتب) فلا يُعاد ضبطها كل مرة. وزر «فحص تشخيصي» يسمّي الملف الذي سيفحصه بدل أن يترك المستخدم يخمّن. والتأكيد قبل الكتابة فوق ملفات موجودة، وقبل تفريغ قائمة تجاوزت ثلاثة ملفات.

### ٣٫٣ الاستجابة — **7/10**

نقاط التوقف مضبوطة (٩٠٠px لطيّ العمودين، ٥٦٠px لتكديس الترويسة)، والجدول داخل `.table-wrap { overflow-x: auto }`، والرقاقة عولجت للتوّ في الالتزام `50b1181` لتمنع فيضان ترويسة الجوال.

**لكن الفيضان نفسه ما زال قائمًا في صفّ الأزرار الرئيسي**، ومن نفس السبب:

> `web/js/app.js` — في `syncButtons()`:
> ```js
> $('btnDiag').textContent = has ? `فحص: ${state.files[0].file.name}` : 'فحص تشخيصي';
> ```
> `web/css/styles.css`:
> ```css
> .row { display: flex; gap: 8px; align-items: center; }   /* بلا flex-wrap */
> .row.wrap { flex-wrap: wrap; }                            /* صنف لا يحمله صفّ الأزرار */
> ```

صفّ الأزرار في `index.html` هو `<div class="row">` بلا `wrap`، وفيه ثلاثة أزرار أحدها يحمل **اسم الملف كاملًا**. واسم مثل «مذكرة جوابية في الدعوى العمالية رقم ١٢٣٤٥ لعام ١٤٤٧هـ.pdf» — وهو اسم واقعي تمامًا لجمهور هذه الأداة — يتجاوز عرض شاشة ٣٦٠px بأضعاف، فيفيض الصفّ ويظهر تمرير أفقي للصفحة كلها.

**الحل:**
```css
.actions .row { flex-wrap: wrap; }
#btnDiag { min-width: 0; max-width: 100%; overflow: hidden;
           text-overflow: ellipsis; white-space: nowrap; }
```

### ٣٫٤ العربية و RTL — **7.5/10**

الأساس سليم: `<html lang="ar" dir="rtl">`، و`setLayoutDirection(RightToLeft)` على مستوى `QApplication` والنافذة معًا، وخطوط `Amiri`/`Cairo` بسلاسل بدائل مضمونة، وقرار موفّق بإخراج المعاينة والسجل من الخط الأحادي اللاتيني «لانعدام تغطيته العربية».

**العيب: شكل الأرقام مختلط داخل اللوحة الواحدة.** في نفس تبويب التشخيص:

> `web/js/app.js`:
> ```js
> ['رباطات مُصلَحة في العيّنة', d.ligatures.toLocaleString('ar-EG')]   // → ١٬٢٣٤ هندية
> ...
> td.textContent = String(cell);                                        // → 1234 غربية
> ...
> say(`✓ ${result.name} (${result.stats.chars.toLocaleString('ar-EG')} حرف …`)  // → هندية
> ...
> function human(bytes) { return `${bytes} بايت`; }                     // → غربية
> ```

فالمستخدم يرى «رباطات مُصلَحة: **١٬٢٣٤**» بالهندية فوق جدول أرقامه **غربية** مباشرةً، وحجم الملف غربيًّا وعدد الحروف هنديًّا. والمفارقة أن الأداة نفسها تعرض خيار «توحيد الأرقام الهندية ← عربية» على محتوى المستند بينما واجهتها لا تُوحّدها. وسطر الأوامر والواجهة الرسومية يستعملان `f"{x:,}"` أي غربية دائمًا — فالواجهات الثلاث لا تتفق.

**الحل:** اختر شكلًا واحدًا لكل الواجهة. الأنسب للاتساق مع الواجهتين الأخريين ومع ناتج الأداة: `toLocaleString('ar-SA-u-nu-latn')` أو `'en-US'` في كل المواضع الأربعة.

**ملاحظة أدنى:** `.files .name { direction: ltr; text-align: right }` — الاتجاه اللاتيني على اسم ملف عربي يقلب موضع الامتداد والأقواس بصريًّا. مقصود لإظهار `.md`/`.pdf` في الطرف المتوقَّع، لكنه يستحق `unicode-bidi: plaintext` بدلًا منه ليتبع كل اسم اتجاهه.

### ٣٫٥ إمكانية الوصول — **7/10**

المنجَز حقيقي ومحسوب: رابط «تخطَّ إلى المحتوى»، و`aria-label` على كل زر أيقونة، و`role="status"` و`aria-live` على لوحة الإقلاع، و`setBuddy` في Qt ليعلن قارئ الشاشة اسم الحقل، واختصار `Delete` على القائمة، و`:focus-visible` بلون رُفع خصّيصًا ليتجاوز 3:1، وجدول التشخيص لا يعتمد على اللون وحده (رمز `✓`/`✗` مع اللون).

**لكن حسابي لتباين اللوحتين كشف ثلاث مخالفات ما زالت قائمة** (معادلة WCAG، النسب مقيسة لا مقدَّرة):

| العنصر | اللون على الخلفية | النسبة | المطلوب | الحكم |
|---|---|---|---|---|
| **حكم التشخيص الناجح** `.verdict.ok` | `#2e7d52` على `#f3eada` | **4.22:1** | 4.5:1 (SC 1.4.3) | ✗ فاتح |
| **حدّ حقول الإدخال (داكن)** `--field-line` | `#6f6047` على `#241d16` | **2.73:1** | 3:1 (SC 1.4.11) | ✗ داكن |
| **تعبئة شريط التقدّم (فاتح)** `--gold` | `#c9a227` على `#fffdf8` | **2.38:1** | 3:1 (SC 1.4.11) | ✗ فاتح |

الثانية ملاحظة مهمة تحديدًا: المراجعة الثالثة رفعت حدّ الحقول في اللوحة الفاتحة من 1.44:1 إلى **3.31:1** ✓ — ثم وُلد المظهر الداكن بعدها ولم يُقَس، فعاد العيب نفسه في اللوحة الجديدة.

والثالثة ليست زخرفية كما يفترض التعليق في `gui.py`: **تعبئة الشريط هي المعلومة الوحيدة** التي تنقل التقدّم، فتنطبق عليها SC 1.4.11 لا استثناء الزخرفة. (في المظهر الداكن النسبة 8.22:1 ✓.)

**قيم بديلة محسوبة وجاهزة:**
```css
--ok: #2a7049;          /* 5.01:1 على --head  ✓  (بدل 4.22) */
[data-theme='dark'] { --field-line: #7d6d52; }   /* 3.31:1 على --panel ✓ */
.bar-fill { background: var(--gold-focus); }      /* 3.73:1 على --panel ✓ */
```

**مخالفة رابعة — حجم هدف اللمس:** زر حذف الملف من القائمة:
```css
.files .x { font-size: 18px; line-height: 1; padding: 0 4px; }   /* ≈ 19×18 بكسل */
```
دون **24×24** التي تفرضها WCAG 2.2 SC 2.5.8 — وهو الزر الوحيد لإزالة ملف من القائمة على الجوال. **الحل:** `min-width: 28px; min-height: 28px; display: grid; place-items: center;`

**ملاحظة أخيرة:** الألسنة تحمل `role="tab"` و`aria-selected` صحيحين، لكن بلا تنقّل بالأسهم ولا إدارة `tabindex` كما تفرض ممارسات WAI-ARIA لنمط `tablist`. عملية لا حرجة (الأزرار قابلة للوصول بـTab).

### ٣٫٦ التغذية الراجعة — **7.5/10**

من أقوى جوانب المشروع: شريط إقلاع بنسبة ورسالة لكل مرحلة، وتقدّم صفحةً صفحة، وسجل كامل، وحكم تشخيصي بلغة بشرية، ورسائل خطأ عربية كلها (حتى `traceback` يُمسَك عند حدّ اللغتين في `web.convert_file` «فيصل المستخدم العربي traceback بالإنجليزية بدل رسالة مفهومة»).

**عيبان:**

**(أ) نتائج الدفعات تتراكم بلا تصفير.** في `app.js`، `state.results` يُدفَع إليه في السطر 453 و**لا يُصفَّر في أي موضع** — لا في `runBatch()` ولا في زر «تفريغ القائمة». والواجهة الرسومية تفعل العكس (`self.results = []` في `_run_convert`). فمن حوّل ملفًا ثم غيّر خيارًا وأعاد التحويل يجد **اسمين متطابقين** في قائمة المعاينة بلا ما يميّزهما، و«تنزيل الكل .zip» يسلّمه `ملف.md` و`ملف-2.md` — والفكّاك لا يعرف أيهما الأحدث. (دالّة فضّ التعارض في `zip.js` مكتوبة أصلًا لأن هذه الحالة تقع.)

**الحل:** `state.results = []` في مطلع `runBatch()`، أو زر «تفريغ النتائج» صريح.

**(ب) التجمّد الصامت.** عند مسارَي التجمّد في البند ٢٫٤، الرسالة الوحيدة سطرٌ في تبويب **السجل** (`say('✗ ' + msg)`) — وشريط الحالة والأزرار تبقى كما هي. فالمستخدم يرى واجهة تقول «جاهز» ولا تستجيب، بلا أي إشارة إلى أن عليه تحديث الصفحة. **الحل:** عند `fatal`، اعرض في `status()` واكسر `state.running`.

### متوسط قسم UX/UI

| النقطة | الدرجة |
|---|---|
| الاتساق البصري | 9.0 |
| سهولة الاستخدام | 9.0 |
| الاستجابة | 7.0 |
| العربية و RTL | 7.5 |
| إمكانية الوصول | 7.0 |
| التغذية الراجعة | 7.5 |

**المتوسط: 7.8/10**

---

## 4. المراجعة الأمنية (25%)

**سياق الحكم:** أداة **محلية بالكامل** بلا خادم ولا حسابات ولا قاعدة بيانات. فبنود «الصلاحيات والمصادقة» و«الجلسات» و«حقن SQL» **لا تنطبق** ولا تُخترَع لها ثغرات. لكن للمشروع سطحان حقيقيان يستحقان التدقيق: **تحليل ملفات PDF غير موثوقة عبر مكتبة C**، و**صفحة ويب تستضيف مفسّرًا كاملًا وتعالج وثائق قد تكون سرّية**.

### ٤٫١ المفاتيح والأسرار — **10/10** ✓

مسح شامل على `*.py` و`*.js` و`*.json` و`*.yml`: **لا مفتاح ولا رمز ولا سرّ**. المطابقات الوحيدة أسماء ثوابت (`MIN_LATIN_TOKENS`) وتعليقات تشرح أن `GITHUB_TOKEN` **لا يُستعمل**. و`.gitignore` يستبعد `web/vendor/` و`_site/`.

### ٤٫٢ الحقن — **9.5/10** ✓

- **لا `innerHTML` ولا `eval` ولا `insertAdjacentHTML` ولا `document.write` في المشروع كله** — مؤكَّد بالمسح. كل بناء DOM بـ`createElement` + `textContent`، بما في ذلك جدول التشخيص الذي يعرض بيانات وصفية من ملف PDF **غير موثوق** (`producer`/`creator`).
- الواجهة الرسومية تصيّر `RichText` لكنها تُهرّب المدخل الخارجي: `html.escape(d['producer'] or '—')` — مُعالَج في مراجعة سابقة وما زال سليمًا.
- `subprocess.run(["tesseract", "stdin", "stdout", "-l", language, ...])` بصيغة قائمة بلا `shell=True` — فقيمة `--ocr-lang` لا تُفسَّر كأمر صدفة.
- `zip.js` يبني الأرشيف بايتًا بايت بلا أي تنفيذ، ويضبط راية `0x0800` لأسماء UTF-8. (تحقّقتُ من إزاحات الترويسة المحلية والدليل المركزي و`EOCD` — كلها مطابقة للمواصفة.)

### ٤٫٣ التحقق من المدخلات — **8/10**

**قوي:** `web.build_options()` يقصّ الأعداد على حدود صريحة ويرفض القيم غير المعروفة للحقول النصّية، بتعليق يشرح السبب تمامًا: «ما يصل من جافاسكربت مُدخَل لا يُوثق به: عنصر HTML مُعدَّل من أدوات المطوّر… تصل إلى هنا كما هي». و`argparse` يقيّد `--h-top` بـ`range(1,7)`، و`--ocr-dpi` دون ٢٠٠ يُرفض صراحةً لأنه «فساد صامت لا رسالة خطأ». والملف المحمي بكلمة مرور يُرفض قبل أي معالجة. و`INK_MAX_PIXELS` يحرس من قنبلة تكبير الصفحة.

**واستخراج الأرشيف آمن قصدًا** — `tools/fetch_web_runtime.py`:
```python
name = os.path.basename(member.name)
if not member.isfile() or name not in CORE_FILES: continue
```
انتقاء بالاسم لا بالمسار، مع تعليق يشرح خطر `../x` في `tarfile` على بايثون ٣٫٩. هذا وعيٌ أمني حقيقي.

**الناقص:** لا حدّ ولا تحذير لحجم ملف الدخل في المتصفّح. `engine.run()` يقرأ `await file.arrayBuffer()` لأي حجم، ثم يُكتب في `MEMFS` — فملف بمئات الميغابايت يقتل الخيط بنفاد الذاكرة، **وهو الانهيار الذي يُفضي إلى التجمّد الدائم في البند ٢٫٤(ب)**. أي أن الثغرة الوظيفية والنقص في التحقق من المدخلات يلتقيان في نفس المسار: مستخدم يُفلت ملفًا كبيرًا فتموت الصفحة بلا رسالة.

**الحل:** حدّ ليّن في `addFiles()` (~١٥٠ م.ب) برسالة صريحة تقترح سطر الأوامر للملفات الأكبر — لا رفضًا صلبًا.

### ٤٫٤ الصلاحيات والمصادقة — **لا ينطبق**

لا خادم ولا حسابات ولا جلسات ولا مسارات محمية. لا تُحتسب في المتوسط.

### ٤٫٥ البيانات الحساسة — **9.5/10** ✓

هذا أقوى ما في القسم الأمني، لأن الوعد **مفروض تقنيًا لا مكتوب لفظًا**:

```html
<meta http-equiv="Content-Security-Policy"
  content="default-src 'self'; script-src 'self' 'wasm-unsafe-eval'; style-src 'self';
           img-src 'self' data:; connect-src 'self'; worker-src 'self' blob:;
           base-uri 'self'; form-action 'none';">
```

`connect-src 'self'` يعني أن **الصفحة لا تملك إذن المتصفّح** لإرسال ملف المستخدم إلى أي مكان — والأداة موجَّهة لمذكرات ومرافعات قد تكون سرّية، فالفرق بين «نعدك ألّا نرفع» و«لا نستطيع الرفع» فرق جوهري. و`form-action 'none'` و`base-uri 'self'` تسدّان مسارَي تسريب أقلّ شهرة. ولا تُخزَّن في `localStorage` إلا تفضيلات الواجهة — لا محتوى مستند ولا أسماء ملفات.

وزمن التشغيل **مستضاف ذاتيًا بلا شبكة توصيل محتوى**، وكل ملف يُتحقّق من بصمة `sha256` قبل استعماله (`tools/fetch_web_runtime.py`)، بتعليق يشرح المخاطرة: «الأداة تُشغَّل من متصفّح المستخدم، فحزمة مبدَّلة في الطريق تعني تنفيذ شيفرة غريبة على مستنداته». هذا سلسلة توريد محكومة فعليًا لا ادّعاءً.

### ٤٫٦ الاعتماديات — **8.5/10**

`pip-audit -r requirements.txt` → **No known vulnerabilities found** (مقيس الآن). والأرضية مرفوعة عمدًا بتعليل صريح: «الأداة تحلّل ملفات PDF غير موثوقة عبر MuPDF وهي مكتبة C، فأخطاء التحليل فيها تعني تلف ذاكرة لا استثناء بايثون». وDependabot أسبوعي على pip وشهري على الإجراءات. وحدود عليا `<2` و`<3` تمنع سحب إصدار رئيسي كاسر. و`main.py` يحذّر من إصدار PyMuPDF القديم بدل أن يرفضه.

**الخصم:** `run.sh` و`run.bat` ما زالا يثبّتان على مستوى النظام ثم المستخدم بلا بيئة معزولة — نُبّه عليه في مراجعتين سابقتين، وهو الآن موثّق صراحةً في README كمقايضة مقصودة للجمهور غير التقني. الاعتراف يخفّف اللوم ولا يزيله: `pip install` عام يخلط اعتماديات المشروع بغيرها وقد يُرقّي حزمة يعتمد عليها شيء آخر على جهاز المستخدم.

### ٤٫٧ CORS والترويسات — **6.5/10**

`connect-src 'self'` يسدّ الخروج، لكن **الدخول غير مسدود**:

- **`frame-ancestors` مفقودة، ولا يمكن إضافتها عبر `<meta>` أصلًا** — المواصفة تنصّ على أن `frame-ancestors` و`sandbox` و`report-uri` **تُتجاهَل** حين تُسلَّم بوسم `meta`. فالصفحة قابلة للتأطير (`iframe`) من أي موقع، وهجوم اختطاف النقر (clickjacking) على صفحة يُفلت فيها المستخدم وثيقة سرّية ليس مخاطرة نظرية بحتة.
- **`vercel.json` بلا قسم `headers` إطلاقًا** — وهي المنصّة الوحيدة من المنصّتين التي **تستطيع** إرسال ترويسات حقيقية.
- **فرصة ضائعة:** لو أُرسلت `COOP`/`COEP` من Vercel لأمكن استعمال `SharedArrayBuffer`، فيصير «الإيقاف» مقاطعةً حقيقية بدل قتل الخيط وإعادة إقلاعه — وهو بالضبط القيد الذي يشرحه `worker.js` ويعزوه إلى GitHub Pages.

**الحل** — في `vercel.json` (مع الانتباه إلى تحذير README أن المخطَّط يرفض المفاتيح المجهولة، و`headers` مفتاح معروف صحيح):
```json
"headers": [{ "source": "/(.*)", "headers": [
  { "key": "Content-Security-Policy", "value": "frame-ancestors 'none'" },
  { "key": "X-Content-Type-Options", "value": "nosniff" },
  { "key": "Referrer-Policy", "value": "no-referrer" },
  { "key": "Cross-Origin-Opener-Policy", "value": "same-origin" },
  { "key": "Cross-Origin-Embedder-Policy", "value": "require-corp" }
]}]
```
أما GitHub Pages فلا يرسل ترويسات مخصّصة بحال — والصدق أن يُذكر هذا الحدّ في README بدل السكوت عنه.

**نقطة إيجابية:** صلاحيات سير العمل مضبوطة على الأقل الممكن (`contents: read` في `tests.yml` مع تعليل)، وهو انضباط نادر.

### متوسط القسم الأمني

| النقطة | الدرجة |
|---|---|
| المفاتيح والأسرار | 10.0 |
| الحقن | 9.5 |
| التحقق من المدخلات | 8.0 |
| الصلاحيات والمصادقة | *لا ينطبق* |
| البيانات الحساسة | 9.5 |
| الاعتماديات | 8.5 |
| CORS والترويسات | 6.5 |

**المتوسط على المنطبق (٦ بنود): 8.7/10**

---

## 5. الدرجة الإجمالية المرجّحة

| الناحية | الدرجة | الوزن | المساهمة |
|---|---|---|---|
| تقنية وهندسية | 7.8/10 | 45% | 3.510 |
| UX/UI | 7.8/10 | 30% | 2.340 |
| أمن سيبراني | 8.7/10 | 25% | 2.175 |
| **الإجمالي** | | | **8.03/10** |

**الحساب صراحةً:** (7.8 × 0.45) + (7.8 × 0.30) + (8.7 × 0.25) = 3.510 + 2.340 + 2.175 = **8.03 ≈ 8.0/10**

### الحركة عبر المراجعات الأربع

| القسم | الأولى | الثانية | الثالثة | **الرابعة** | القراءة |
|---|---|---|---|---|---|
| هندسة | — | — | 8.3 | **7.8 ▼** | الشيفرة تحسّنت، لكن مساحة جديدة (OCR + ويب) دخلت بعيب صحّة حرج وثغرتَي تجمّد |
| UX/UI | — | — | 7.4 | **7.8 ▲** | مظهر داكن، ورابط تخطٍّ، و`:focus-visible`، واستجابة حقيقية — مقابل مخالفات تباين جديدة في اللوحة الداكنة |
| أمن | — | — | 7.5 | **8.7 ▲** | قفزة حقيقية: CSP يفرض الخصوصية، وبصمات sha256 على سلسلة التوريد، وصلاحيات CI مقيّدة، و`pip-audit` نظيف |
| **الإجمالي** | 7.5 | 7.9 | 7.8 | **8.0 ▲** | |

**الحكم بصراحة:** هذا مشروع **فوق المتوسط بكثير** هندسيًّا — التعليقات، والاختبارات، والفصل الطبقي، ووعي سلسلة التوريد كلها في مستوى لا تبلغه أغلب المشاريع المفتوحة. والدرجة لم ترتفع أكثر لسبب واحد محدَّد: **مساحة العام الجديدة (رصد OCR + واجهة الويب) لم تنل من صرامة الاختبار ما نالته المساحة القديمة.** `ocr.py` عند ٥٥٪ تغطية أخفى عيبًا يضرب صنفًا شائعًا من المستندات، وجافاسكربت بلا اختبارات إطلاقًا أخفى مسارَي تجمّد دائم.

الخبر الطيّب أن العيب الحرج **إصلاحه سطر واحد**، وأن الثغرتين في `engine.js` إصلاحهما خمسة أسطر. البنية سليمة تمامًا ولا تحتاج إعادة كتابة ولا إعادة هيكلة — تحتاج أن يلحق الاختبار بالمساحة الجديدة.

---

## 6. خطة الإصلاح المرتبة بالأولوية

| # | الأولوية | الناحية | المشكلة | الحل المقترح | التأثير المتوقع بعد الحل |
|---|---|---|---|---|---|
| 1 | 🔴 حرج | هندسة | `ocr._body_chars()` تقرأ `span.get("text", "")` وهو مفتاح **لا وجود له في `rawdict`** (مؤكَّد على PyMuPDF 1.28.2) — فترجع **صفرًا دائمًا**، فينهار حدّ الصفحة الممسوحة من `IMG_COVER=0.50` إلى `IMG_COVER_EMPTY=0.10`. مقيس: صفحة فيها ١٩١٤ حرفًا سليمًا + شعار يغطي ١٣٪ → `page_verdict = (True, "صفحة ممسوحة ضوئيًا بلا طبقة نص")` | في `src/ocr.py`، استبدل حلقة `for ch in span.get("text", "")` بـ`for ch in span.get("chars") or []` واعدد `not ch["c"].isspace()` (مُتحقَّق: 0 → 1530) | المستندات ذات الترويسات والأختام — وهي **الشكل الغالب لجمهور الأداة** — تتوقّف عن رمي طبقة نصها السليمة في OCR، ويتوقّف التشخيص عن طرد مستخدم المتصفّح إلى سطر الأوامر بلا سبب |
| 2 | 🔴 حرج | هندسة | `engine.js` — مسارا **تجمّد دائم** أُثبتا بالتنفيذ: (أ) `cancel()` لا تصفّر `about`، فحارس `!this.about` في `#crash` يمنع رفض إقلاع بديل فاشل؛ (ب) `#crash` تُبقي الخيط الميت مرجَّعًا و`ready` محسومًا، فالمهمة التالية تُرسَل إلى خيط لا يردّ | في `cancel()`: `this.about = null;` — وفي `#crash()`: أنهِ الخيط (`terminate()`) وصفّر `worker` و`ready` وارفض `bootReject` بلا شرط. أضف مهلة حراسة ٦٠ ثانية على كل مهمة | انهيار نفاد الذاكرة (وهو ما يحذّر منه README نفسه) يصير رسالة خطأ وإقلاعًا جديدًا، بدل صفحة «جاهزة» لا تستجيب إلا بالتحديث |
| 3 | 🟠 مهم | أداء | `structure._extract_pages()` يحسب `ready` ثم يستدعي `page_verdict()` بغضّ النظر عنه، و`page_verdict` تقرأ الصفحة ٣–٤ مرات. مقيس على ٤٠ صفحة بلا Tesseract: **+٧٥٪** زمنًا (٠٫٤٤ → ٠٫٧٧ ث) ثمنًا لقرار لا يمكن تنفيذه | `if ready or opt.ocr == "always":` قبل استدعاء `page_verdict`، مع تحذير واحد للمستخدم؛ و`opt.ocr = "never"` صراحةً في `web.build_options()` (لا Tesseract في Pyodide بحال) | كل مستخدم بلا Tesseract — وهو الافتراضي، و**دائمًا** في المتصفّح — يستعيد ٤٠٪ من زمن التحويل فورًا |
| 4 | 🟠 مهم | هندسة | `ocr.broken_yeh_hits()` تصف الياء المكسورة وصفًا مخالفًا لـ`core._is_yeh_pair()`: تُهمل `U+064B` (سلبية كاذبة) وتُهمل شرط الموضع فتحسب **التشكيل الطائر** شاهدًا (إيجابية كاذبة) — والعتبة ٣ فقط، أي ثلاث كلمات تنتهي بـ«ـًا» تُرسل صفحة سليمة كلها إلى OCR | اجعل `broken_yeh_hits` تستدعي `core._is_yeh_pair(prev, span["size"], char, span["size"])` بدل إعادة كتابة الشروط — مصدر حقيقة واحد للظاهرة الواحدة | يزول ثاني مسار لإتلاف طبقة نص سليمة، ويصير الراصد والمُصلِح مضمونَي الاتفاق إلى الأبد |
| 5 | 🟠 مهم | اختبارات | `ocr.py` عند **٥٥٪** — و`_body_chars`/`page_verdict`/`_image_cover` **بلا تغطية إطلاقًا**، ولا اختبار يمرّر صفحة PyMuPDF حقيقية على `page_verdict`. لذلك مرّ البند ١ من CI أخضر | `tests/test_integration.py`: صفحة نصية كثيفة + شعار صوري ≥١٠٪ → `assert page_verdict(page) == (False, "")`؛ وصفحة صورة خالصة → `assert page_verdict(page)[0] is True`. وأضف `"text"` إلى نفي `RAW_KEYS` صراحةً | البند ١ يصير مستحيل التكرار، وأضعف وحدة في المشروع تخرج من العمى |
| 6 | 🟠 مهم | وصول | ثلاث مخالفات تباين مقيسة: `.verdict.ok` = **4.22:1** (المطلوب 4.5)؛ `--field-line` الداكن = **2.73:1** (المطلوب 3)؛ تعبئة شريط التقدّم الفاتحة = **2.38:1** (المطلوب 3، وهي المعلومة الوحيدة للتقدّم لا زخرفة) | في `styles.css`: `--ok: #2a7049` (5.01:1 ✓)، و`[data-theme='dark'] --field-line: #7d6d52` (3.31:1 ✓)، و`.bar-fill { background: var(--gold-focus) }` (3.73:1 ✓) — وانقل الأولى إلى `QSS` في `gui.py` حفاظًا على وحدة اللوحة | المظهر الداكن يلحق بالفاتح في مطابقة WCAG بدل أن يعيد عيبًا أُصلح قبله، وحكم التشخيص يُقرأ فعلًا |
| 7 | 🟠 مهم | UX/استجابة | صفّ الأزرار `<div class="row">` بلا `flex-wrap`، و`syncButtons()` تحشو `btnDiag` باسم الملف كاملًا — فاسم واقعي كـ«مذكرة جوابية في الدعوى العمالية رقم ١٢٣٤٥.pdf» يفيض شاشة الجوال ويُحدث تمريرًا أفقيًا للصفحة كلها | `.actions .row { flex-wrap: wrap }` مع `#btnDiag { min-width:0; max-width:100%; overflow:hidden; text-overflow:ellipsis; white-space:nowrap }` | العلاج نفسه الذي أُعطي للرقاقة في `50b1181` يُعطى للصفّ الذي يحمل الفعل الأساسي |
| 8 | 🟠 مهم | UX | `state.results` في `app.js` يُدفَع إليه ولا يُصفَّر أبدًا — لا في `runBatch()` ولا في «تفريغ القائمة» (والواجهة الرسومية تصفّره). فإعادة التحويل بعد تغيير خيار تُنتج اسمين متطابقين في المعاينة، و«تنزيل الكل» يسلّم `ملف.md` و`ملف-2.md` بلا ما يميّز الأحدث | `state.results = []` في مطلع `runBatch()`، أو زر «تفريغ النتائج» صريح بجوار «تفريغ القائمة» | المعاينة تعرض نتيجة التشغيل الأخير لا أرشيفًا متراكمًا، ويكفّ الأرشيف عن حمل نسخ لا يُعرف ترتيبها |
| 9 | 🟠 مهم | أمن | `frame-ancestors` غائبة — ولا يمكن تسليمها عبر `<meta>` أصلًا (تتجاهلها المواصفة)، و`vercel.json` بلا قسم `headers` إطلاقًا رغم أن Vercel المنصّة الوحيدة القادرة على إرسال ترويسات | أضف `headers` في `vercel.json`: `frame-ancestors 'none'` و`nosniff` و`no-referrer` — وأضف `COOP`/`COEP` لتفتح باب `SharedArrayBuffer` أي إيقافًا حقيقيًا بلا قتل الخيط. وسجّل في README أن GitHub Pages لا يرسل ترويسات مخصّصة | صفحة تُفلَت فيها وثائق سرّية تُحصَّن من التأطير على المنصّة التي تسمح بذلك، ويُفتح طريق لإيقاف لا يُعيد الإقلاع |
| 10 | 🟡 تحسين | عربية/UX | شكل الأرقام مختلط داخل اللوحة الواحدة: `toLocaleString('ar-EG')` (هندية) للرباطات وعدد الحروف، بينما جدول التشخيص وحجم الملف غربية — وسطر الأوامر والواجهة الرسومية غربية دائمًا | وحّد المواضع الأربعة في `app.js` على `toLocaleString('en-US')` (أو `ar-SA-u-nu-latn`) اتساقًا مع الواجهتين الأخريين ومع ناتج الأداة نفسه | الواجهات الثلاث تتكلّم لغة رقمية واحدة، وتختفي مفارقة أداةٍ تعرض «توحيد الأرقام» ولا توحّدها في واجهتها |
| 11 | 🟡 تحسين | وصول | زر حذف الملف `.files .x` ≈ **19×18 بكسل** — دون 24×24 التي تفرضها WCAG 2.2 SC 2.5.8، وهو الوسيلة الوحيدة لإزالة ملف من القائمة على الجوال | `.files .x { min-width: 28px; min-height: 28px; display: grid; place-items: center; }` | هدف اللمس يصير قابلًا للإصابة على الجوال بدل أن يحتاج دقّة الفأرة |
| 12 | 🟡 تحسين | اختبارات | حارس تكافؤ `Options` موجود للواجهة الرسومية (`test_every_boolean_option_has_a_checkbox`) وغائب عن الويب — فخيار منطقي جديد سيغيب عن `web.BOOL_FIELDS` بصمت (المطابقة اليوم صحيحة، مقيسة) | انسخ الاختبار نفسه إلى `tests/test_web.py` مقابل `web.BOOL_FIELDS` — ثلاثة أسطر | الواجهة الثالثة تُحرَس كما تُحرَس الثانية، فيستحيل أن يوجد خيار في اثنتين ويغيب عن ثالثة |
| 13 | 🟡 تحسين | أمن/UX | لا حدّ ولا تحذير لحجم الملف في المتصفّح؛ `engine.run()` يقرأ أي حجم إلى الذاكرة ثم إلى `MEMFS` — وهو المسار المؤدّي إلى انهيار الخيط في البند ٢ | حدّ ليّن في `addFiles()` (~١٥٠ م.ب) برسالة تقترح سطر الأوامر للأكبر، لا رفضًا صلبًا؛ وسطر في «حدود واجهة الويب» في README | المستخدم يعرف الحدّ قبل أن يصطدم به، لا بعد أن تموت الصفحة صامتة |
| 14 | 🟡 تحسين | هندسة | `gui.py` ٩٨٨ سطرًا (٣٠٪ من مصدر بايثون)، و`MainWindow` تحمل ستّ مسؤوليات، و`_options_box()` ~١٠٠ سطر بعدّاد `r` يُزاد اثنتي عشرة مرة يدويًا | اقسم إلى `MainWindow` + `OptionsPanel` (يملك `options()` و`_checkboxes()`) + `BatchRunner` (يملك `queue` و`_workers`) | أكبر ملف في المشروع ينزل تحت ٦٠٠ سطر، وتصير لوحة الخيارات قابلة للاختبار وحدةً مستقلة |
| 15 | 🟡 تحسين | أداء | `page_verdict` و`page_lines` تحلّلان الصفحة نفسها بـ`rawdict` مرتين مستقلتين (فوق `get_text` و`dict`) | مرّر مخرَج `rawdict` **مرة واحدة** إلى الاثنتين بدل إعادة التحليل لكل فحص | ما يتبقّى من ضريبة OCR بعد البند ٣ يزول أيضًا لمن **يملك** Tesseract |
| 16 | 🟡 تحسين | وصول | ألسنة `role="tab"` بلا تنقّل بالأسهم ولا إدارة `tabindex` كما تفرض ممارسات WAI-ARIA لنمط `tablist` | مستمع `keydown` على `.tabs` ينقل التركيز بـ`ArrowRight`/`ArrowLeft` (منعكسَين في RTL)، مع `tabindex="-1"` لغير المحدَّد | نمط الألسنة يوفي عقده المعلَن بدل أن يعلن دورًا لا يسلك سلوكه |
| 17 | 🟡 تحسين | هندسة | `tools/fetch_web_runtime.py` — `urllib.request.urlopen(url)` بلا `timeout`، فاتصال متوقّف يعلّق وظيفة النشر إلى أن تنتهي مهلة GitHub Actions | `urlopen(url, timeout=60)` مع رسالة عربية عند الانتهاء | نشرة تفشل بسبب واضح في دقيقة، بدل وظيفة معلّقة ست ساعات |

---

## خلاصة في ثلاثة أسطر

**PDF2MD مشروع هندسي ممتاز فيه عيب صحّة واحد حرج.** الفصل الطبقي والتعليقات المعلَّلة بالقياس والاختبارات الـ١٨٨ ووعي سلسلة التوريد كلها فوق المتوسط بكثير، وواجهة الويب إنجاز حقيقي: نفس محرّك بايثون يعمل في المتصفّح بلا نسخة ثانية، وبخصوصية يفرضها المتصفّح لا نصّ وعد.

والعيب الحرج — `span.get("text")` على `rawdict` — يجعل **كل** صفحة تحمل شعارًا أو ختمًا يغطي ١٠٪ منها تُحكَم «ممسوحة ضوئيًا بلا نص»، فتُرمى طبقة نصها السليمة في OCR أدنى منها، أو يُطرد مستخدم المتصفّح إلى سطر الأوامر بلا سبب. إصلاحه **سطر واحد**، ومنع تكراره **اختباران**.

النمط الجامع للملاحظات كلها واحد: **الاختبار لم يلحق بالمساحة الجديدة.** `ocr.py` عند ٥٥٪ أخفى عيبًا حرجًا، وجافاسكربت بلا اختبار إطلاقًا أخفى مسارَي تجمّد دائم، والمظهر الداكن وُلد بعد قياس التباين فورث عيبًا أُصلح في الفاتح. البنية سليمة ولا تحتاج إعادة كتابة — تحتاج أن يمتدّ إليها ما يحرس القديم.
