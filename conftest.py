# -*- coding: utf-8 -*-
"""
موجود عمدًا وفارغ من الكود — لا تحذفه.

وجود conftest.py في جذر المشروع يجعل pytest يضيف الجذر إلى sys.path، فتنجح
`from src import core` في كل ملفات الاختبار. حذفه يكسر الاختبارات كلها دفعةً
واحدة برسالة ModuleNotFoundError غامضة لا تدل على السبب.
"""
